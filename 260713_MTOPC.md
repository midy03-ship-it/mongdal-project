# 260713 모바일 → PC 핸드오프

세션: 모바일(Claude.ai)
주제: 세계관 문서 통합 + 펫 자동수집 예외처리 검토 + 신검 사거리/이펙트 판정 검토 (전부 검토만, 코드 미반영)

---

## 1. 세계관 문서 통합 완료

기존 `WORLDBUILDING.md` v0.4 + 신규로 받은 창세신화 문서(무극/제0법칙/상3계·상4계·하4계)를 통합해서
`WORLDBUILDING_v0.5.md` 생성함. 별도 파일로 출력해뒀으니 확인 후 기존 v0.4 교체 여부 결정 필요.

**통합 구조:**
- 신규 추가: 1장 "창세신화" (무극 → 제0법칙 → 시간축 분리 → 빅바운스 → 법칙의 탄생)
- 8계 정의(원계/황계/어계/선계/현계/유명계/망량계/귀허계)는 신규 문서 쪽이 더 상세해서 교체, 윤회구조(소/중/대윤회) 추가
- 시즌1~8 상세 서사, 경제, NPC, 최종엔딩 등은 기존 v0.4가 이미 상세해서 그대로 유지
- 신규 문서의 "시즌1~3" 서술은 기존 8시즌 상세 구조와 중복되어 통합 과정에서 흡수됨

### To-Do
- [ ] `WORLDBUILDING_v0.5.md` 검토 후 기존 `WORLDBUILDING.md`를 이걸로 교체할지 결정
- [ ] 버전 넘버링(v0.4→v0.5) 커밋 메시지/문서 헤더에 반영

---

## 2. 강다리(구 술신, 개) 자동수집 예외처리 (설계 검토, 코드 미반영)

### 배경
강다리(펫, 오늘 이름 변경)의 "자동수집기" 패시브가 화면에 있는 모든 드랍 아이템을 무차별로 흡수하면,
`SpecialItem` 계열(자석/폭탄/포션)의 "직접 밟아야 전략적으로 발동"하는 기존 설계가 깨짐.

### 검토 결과 — 아이템을 두 그룹으로 분리해서 처리
현재 코드 구조상 아이템이 이미 두 클래스로 나뉘어 있어 분리 처리가 자연스러움:

| 그룹 | 클래스 | 강다리 자동수집 |
|---|---|---|
| 자동흡수형 | `GoldDrop`, `XpOrb`, `SoulDrop`(영혼조각/영혼석), `BigGoldDrop`(강화석/천운석/천령과/태극석/차원석 등 시즌2 특화재화) | ✅ 먹음 |
| 직접픽업형 | `SpecialItem` — magnet(자석), bomb(폭탄), potion(포션/회복) | ❌ 제외 |

**근거:** 자석/폭탄/포션 3종은 "밟는 순간 즉시 발동"하는 전술적 선택 아이템이라, 펫이 대신 밟아버리면
플레이어가 타이밍을 선택할 수 없게 되어 기존 게임성이 훼손됨. 나머지(재화형 드랍)는 전부 자동흡수 대상이라
펫이 대신 주워도 문제 없음.

### To-Do (코드 구현 시)
- [ ] 강다리 자동수집 AI 로직에서 타겟 필터링 시 `SpecialItem` 인스턴스(자석/폭탄/포션) 제외
- [ ] `GoldDrop`/`XpOrb`/`SoulDrop`/`BigGoldDrop`은 전부 타겟 포함
- [ ] 4-3(260711 문서 참고) 술신 자동수집 스펙에 이 예외처리 내용 추가 반영

---

## 3. 신검 사거리 확장 시 캐릭터-이펙트 사이 판정 공백 (설계 검토, 코드 미반영)

### 문제
신검 사거리를 늘리면, 현재 구조상 판정이 "이펙트가 표시되는 위치"에서만 발생하는 것으로 보여
캐릭터와 이펙트 사이 구간에 데미지가 안 들어가는 공백이 생김.

### 해결 방향 — 신검 전용 선분 판정
캐릭터 위치 ~ 이펙트(투사체) 위치를 잇는 선분을 매 프레임 계산해서, 그 선 위에 있는 적도 함께 판정하도록
신검에만 적용되는 예외 로직 추가. 다른 무기(부적/신궁/지팡이/영혼낫)는 기존 방식 그대로 유지 — 신검 전용
플래그(예: `sweepFromPlayer: true`)로 분리해서 다른 무기에 영향 없게 처리.

### To-Do (코드 구현 시)
- [ ] `Projectile`에 신검 전용 플래그 추가 (`sweepFromPlayer` 등)
- [ ] 매 프레임 캐릭터~투사체 위치 선분-원(적 히트박스) 교차 판정 로직 구현
- [ ] 신검 사거리 증가치와 함께 실플레이 체감 확인 (과도한 광역화 여부 체크)

---

## 4. 신검 각성별 칼 개수 증가 (기존 기능, 확인만)

각성 단계에 따라 신검의 칼(블레이드) 개수가 늘어나는 기존 기능. 아래 5·6번 이펙트/판정 검토의 전제.

---

## 5. 다중 칼(블레이드) 이펙트 — 판정을 "최초 이펙트"에 맞추기 (설계 검토, 코드 미반영)

### 문제
칼이 2개 이상일 때(예: "샤삭 샤삭") 이펙트가 겹치며 순차적으로 스폰되는데, 지금은 데미지 판정이
**마지막 이펙트가 나타나는 시점**에 맞춰져 있어서 체감상 타격 딜레이가 발생.

### 해결 방향
각 칼을 개별 투사체로 스폰하고, **칼이 등장하는 그 순간 개별적으로 즉시 판정**하도록 변경.
전부 스폰될 때까지 기다렸다가 한꺼번에 판정하는 현재 방식(추정)을 폐기하고,
칼 하나하나가 자기 등장 시점에 자기 몫의 히트체크를 독립적으로 수행.

### To-Do (코드 구현 시)
- [ ] 신검 다중 블레이드 스폰 로직을 개별 `Projectile` 인스턴스로 분리
- [ ] 각 블레이드의 판정 타이밍을 스폰 시점에 맞춰 개별 처리하도록 수정

---

## 6. 칼 개수 많을 때 이펙트 텀 압축 (설계 검토, 코드 미반영)

### 문제
칼이 6개가 되면 "샤사사사사삭"처럼 개별 텀이 벌어진 채로 재생됨. 원하는 느낌은 "샤사사"처럼
거의 이어지는 빠른 콤보 연출.

### 해결 방향
블레이드 스폰 간격을 블레이드 개수에 반비례하게 조정. 예:
- 2개 → 스폰 간격 0.15초
- 6개 → 스폰 간격 0.05초

전체 콤보 지속시간은 비슷하게 유지하면서, 개수가 많아질수록 체감 텀만 좁아지도록 스케일링.

### To-Do (코드 구현 시)
- [ ] 블레이드 스폰 간격을 `baseInterval / bladeCount` 또는 "고정 총 지속시간 내 균등분배" 방식으로 변경
- [ ] 5번(개별 판정 분리) 작업과 함께 처리하는 게 자연스러움 — 같은 스폰 로직을 건드리는 작업이라 순서 묶어서 진행 추천

---

## 8. 시즌3(망랑계) 일반 몬스터 설계 (신규 확정, 코드/이미지 미착수)

챕터21~30 보스/미들보스 이름은 기존 `SEASON3_8_STAGES.md`에 이미 확정되어 있었음. 이번 세션에서
**일반 몬스터(챕터당 2종)** 설계를 신규로 채움 — 시즌1 패턴(챕터당 잡몹 2종+중간보스+챕터보스)과 동일 구조.

| 챕터 | 일반 몬스터 | 컨셉/패턴 |
|---|---|---|
| 21 혼돈의 입구 | 꼬마도깨비 · 헛개비 | 꼬마도깨비=근접 돌진, 헛개비=순간이동 후 기습 |
| 22 요술의 거리 | 여우불 · 홀린 병사 | 여우불=원거리 화염구, 홀린병사=근접+가끔 랜덤 방향 이동(홀림 연출) |
| 23 글리치의 숲 | 오류령 · 깨진 그림자 | 오류령=순간 텔레포트 이동, 깨진그림자=투명화 반복 |
| 24 뒤틀린 시장 | 저주받은 상품령 · 그림자 상인 | 저주받은상품령=자폭돌진, 그림자상인=거리유지 원거리 |
| 25 망랑계 심층 | 심층 도깨비 · 그림자 짐승 | 심층도깨비=근접 강타(넉백), 그림자짐승=빠른 이속 추격형 |
| 26 잠식된 망랑계 | 오염 도깨비 · 잠식령 | 오염도깨비=처치 시 소형 폭발, 잠식령=근접 데미지 도트(오염 확산) |
| 27 요술에 홀린 세계 | 홀린 여우 · 저주 술사 | 홀린여우=플레이어 조작 반전 유발(근접), 저주술사=원거리 디버프 투사체 |
| 28 혼돈의 소용돌이 | 소용돌이귀 · 혼돈 파편체 | 소용돌이귀=주변 넉백 소용돌이 지속, 혼돈파편체=분열(처치 시 2체로 분열, 1회) |
| 29 글리치 폭풍 | 폭풍 글리치 · 오류 폭풍체 | 폭풍글리치=빠른 순간이동 연속, 오류폭풍체=광역 자폭 |
| 30 망랑계의 왕좌 | 왕좌 수호병 · 분신 도깨비 | 왕좌수호병=탱커형(체력高, 저속), 분신도깨비=소환형(주기적 미니 도깨비 소환) |

**설계 방향:** 시즌2(무겁고 느린 저승 몬스터)와 대비되도록, 시즌3 테마(혼돈/요술/글리치)에 맞춰
텔레포트·분열·소환·조작반전 같은 "예측불가능한 이동패턴"을 몬스터 특성으로 몰아넣음.

### To-Do
- [ ] `GAME_DATA` 몬스터 정의에 챕터21~30 잡몹 10종 스탯/패턴 코드로 구현
- [ ] 기본 몬스터 이미지 10종 제작 필요 (이미지 작업 단계, 아래 10번 참고)

---

## 9. 시즌3 특화시스템 5종 — 수치/디테일 확정 (설계 확정, 코드 미반영)

기존 `MOBILE_IDEAS.md`에 개념만 있던 5종(변신카드/도깨비주사위/혼돈시장/글리치몬스터/복수 애기씨)에
구체 수치를 신규로 확정.

### ① 도깨비 주사위 (결과표)
| 눈 | 확률 | 효과 |
|---|---|---|
| 1 | 1/6 | 저주 — 런 내내 받는피해 +15% |
| 2 | 1/6 | 공격력 -10% |
| 3 | 1/6 | 무효과 (꽝) |
| 4 | 1/6 | 이동속도 +10% |
| 5 | 1/6 | 골드 획득량 +20% |
| 6 | 1/6 | 대박 — 공격력 +20%, 받는피해 -10% (동시 적용) |

### ② 복수 애기씨(분신) 스펙
- 혼돈석 소모: 분신 1체당 30개 (최대 3체 = 90개)
- 분신 스탯: 본체 공격력의 40%, 체력 없음(피격 무시)
- AI: 기존 동료 AI 재활용 (랜덤 이동 + 가장 가까운 적 공격)
- 지속시간: 스테이지 종료까지 유지 (스테이지당 1회 소환 후 계속 존재, 소모형 아님)

### ③ 혼돈 시장 — 로비 상주, 자동전투와 무관
> **확인 완료:** 혼돈 시장은 전투 중 UI가 아니라 **로비 상주 NPC**(대장간/의원당과 동일 위치의 UI).
> 오토모드 전투 진행을 막는 강제 선택 요소가 아니라, 원하는 사람만 로비에서 들르는 선택적 콘텐츠임.

- 스테이지 입장마다 재고 3개 슬롯 랜덤 리셋 (기존 상점 구조 재활용)
- 재고 풀: 낮은 확률로 다른 계 재화(강화석/영혼석 등)도 등장 가능 (혼돈=질서 없음 컨셉)
- 가격: 혼돈석 기준 10~50 랜덤

### ④ 글리치 몬스터 modifier — 신규 이미지 불필요 (VFX로 처리)
> **확인 완료:** 글리치 몬스터는 신규 몬스터 종이 아니라, 8번에서 설계한 기본 잡몹 10종에
> 랜덤 버프를 얹는 방식. 효과는 색조 틴트/파티클/크기 스케일/알파 점멸 같은 **VFX로 표현 가능**해서
> 별도 이미지 제작이 필수는 아님. "분열" modifier의 미니 사이즈 버전도 기존 이미지를 코드로 축소
> 렌더링하면 되므로 이미지 불필요.

스테이지 진입 시 몬스터 전체(또는 랜덤 일부)에 1~2개 랜덤 부여:
| modifier | 효과 |
|---|---|
| 빠름 | 이동속도 +40% |
| 느림+거대 | 이동속도 -30%, 크기 +30%, 체력 +50% |
| 분열 | 처치 시 절반체력 미니 몬스터 2체로 분열 (1회만) |
| 투명 | 3초 주기로 1초간 투명화(피격 불가) |

### ⑤ 변신 카드 3종 — 신규 이미지 필요 (카드 아이콘 + 변신 스프라이트)
> **확인 완료:** 이건 신규 이미지가 실제로 필요함. (a) 카드 아이콘 이미지 3장(UI용),
> (b) 변신한 플레이어 스프라이트 3종 — 몬스터 이미지(해골귀·구미호새끼 등 기존 시즌1 몬스터)를
> 그대로 재사용 못 함. 플레이어가 조작하는 캐릭터는 이동방향별 애니메이션이 필요해서 별도 제작 가능성 높음.

| 카드 | 변신 대상 | 공격 패턴 |
|---|---|---|
| 도깨비 카드 | 도깨비 형태 | 근접 강타(넉백 큰 광역) |
| 구미호 카드 | 구미호 형태 | 원거리 화염구 연사 |
| 해골귀 카드 | 해골귀 형태 | 빠른 이속 + 다중 히트 근접 |

드랍률: 처치 시 0.5%, 지속시간 30초, 즉시 사용(재드랍 방지 쿨다운 없음).

### To-Do
- [x] 도깨비주사위 결과표 코드 구현 (2026-07-17, game.js — 시즌3 스테이지 입장 시 1회 랜덤 롤)
- [x] 복수 애기씨 분신 소환 로직 (2026-07-17, game.js — 인게임 HUD 버튼, 혼돈석 30개/체 최대3체)
- [x] 혼돈시장 로비 UI 구현 (2026-07-17, lobby.js — 로비 상주 NPC, 스테이지 입장마다 재고 갱신)
- [x] 글리치 modifier 부여 로직 + VFX 4종 구현 (2026-07-17, spawner.js/enemy.js — 시즌3 챕터 20% 확률)
- [x] 변신카드 드랍/발동 로직 + 카드 UI (2026-07-17, game.js/items.js — 이미지 3종 반입 완료, 방향별 애니메이션 불필요 확인(기존 좌우 플립 재사용))

---

## 10. 시즌3 이미지 제작 목록 (정리, 이미지 작업 단계에서 진행)

| 항목 | 필요 여부 | 수량 |
|---|---|---|
| 기본 잡몹 (8번 표) | ✅ 필요 | 10종 |
| 미들보스/챕터보스 | ✅ 필요 | 20종 (챕터21~30, 미들보스10+챕터보스10) |
| 글리치 modifier VFX | ❌ 불필요 (코드 VFX로 처리) | - |
| 변신카드 아이콘 | ✅ 필요 | 3장 |
| 변신 플레이어 스프라이트 | ✅ 필요 | 3종 (방향별 애니메이션 포함 가능성 높음) |

---

## 11. 초월 이펙트 위치조정 후 기존 무기 위치 어긋남 (버그 리포트, 원인 미확인)

### 증상
어제(260712) 무기 초월(9~10성) 이펙트 인게임 위치를 조정했는데, 이후 초월하지 않은 기존 무기들의
인게임 표시 위치가 어긋남.

### 원인 추정 (미검증)
초월 전용 위치 보정 코드를 추가하면서, 초월 여부와 무관하게 **공유되는 위치 계산 함수**를 함께
건드렸을 가능성. 짐작으로 고치지 않고 CLAUDE.md 원칙대로 시각적 확인 우선.

### To-Do (PC 세션에서 확인 필요)
- [ ] 초월 O/X 두 상태를 나란히 켜놓고 좌표 비교 (실제 렌더 위치 diff 확인)
- [ ] 어제 커밋에서 위치 계산 관련 diff 재검토 — 초월 전용 분기와 공통 분기가 섞였는지 확인
- [ ] 원인 특정 후 초월 전용 코드만 분리해서 기존 무기 영향 없게 수정

---

## 12. 스탯 상세페이지 — 버프 브레이크다운 모달 (설계 검토, 코드 미반영)

### 배경
캐릭터 스탯창에서 공격력 등을 눌렀을 때 최종 숫자만 보이고, 어떤 버프가 얼마씩 기여했는지 안 보임.

### 설계 — 범용 브레이크다운 모달
스탯 항목(공격력/방어력/이동속도/치명타 등) 클릭 시 아래처럼 소스별로 나눠서 표시. 스탯마다 소스
종류가 다르므로 **항목은 가변, 표시 틀만 공통 컴포넌트**로 설계:

| 항목 | 값 |
|---|---|
| 기본 스탯 | `CONFIG.BASE_STATS` 값 |
| 레벨업 자동증가 | `LEVEL_STAT_GAIN × 레벨` |
| 로비 영구강화 | `STAT_UPGRADE_PER_LEVEL × 강화레벨` |
| 서브무기 패시브 | 해당 스탯에 영향 주는 서브무기(예: 업구슬=공격력, 호패=골드/XP) |
| 오행 시너지/삼위일체 | 해당되는 경우 % 보너스 |
| 신목/명부 강화 | 해당되는 경우 |
| **최종 합계** | 실제 적용값 |

### To-Do (코드 구현 시)
- [ ] 스탯별 기여 소스 목록업 함수 설계 (`getStatBreakdown(statKey, player, saveData)` 형태)
- [ ] UI: 스탯 항목 탭 시 브레이크다운 모달/드롭다운 표시
- [ ] 공격력부터 우선 구현, 이후 방어력/이속/치명타 등으로 확장

---

## 13. 삼위일체 발동 토스트 메시지 (설계 검토, 코드 미반영)

### 설계
삼위일체가 **꺼짐→켜짐으로 전환되는 순간에만 1회** 발동. 상태 유지 중에는 스팸 방지, 해제될 때는
별도 알림 없이 조용히 사라짐.

- 메시지 포맷: `"N슬롯 삼위일체 발동!"`
- 재사용 후보: 기존 `_showSlotUnlock` 팝업 시스템 또는 `showFloatingText` 시스템

### To-Do (코드 구현 시)
- [ ] 슬롯별 삼위일체 이전 프레임 상태 저장(`_trinityWasActive[slot]`) → 전환 감지 로직
- [ ] 전환 감지 시 토스트/플로팅텍스트 트리거
- [ ] 2번(오행 시스템) 구현과 함께 처리하는 게 자연스러움 (같은 삼위일체 판정 로직을 건드리는 작업)

---

## 14. 다음 세션 이어갈 것 (업데이트)
- 2~6번 전부 코드 구현 (PC 세션)
- WORLDBUILDING_v0.5.md 확정 후 기존 파일 교체
- 8~10번 시즌3 몬스터/특화시스템 코드 구현
- 11번 초월 위치 버그 원인 확인 (시각적 비교 우선)
- 12~13번 스탯 브레이크다운·삼위일체 토스트 코드 구현
- 15번 보스러시 무한 확장형 재설계 코드 구현
- 19번 동료 잉여 파편→법칙 재화 전환은 시즌6 설계 시점에 상세화 (지금은 방향만 확정)
- (260711 문서에서 이어지는) 이지 슬롯 확장 구현, 오행 시스템 코드 구현 등 기존 우선순위 항목들과 순서 조율

---

## 18. 상사화 사이즈 확정

기존 260706 문서에서 미결이었던 "상사화 사이즈"를 확정:

- **인게임 궤도 표시(펫 장착 시):** `drawH 44px`로 축소, 원본 비율 유지해서 `drawW` 계산 (기존 펫 범위 32~52px 중 상위권 — 유니크 등급 반영)
- **펫 해금 팝업(스테이지200 최초 클리어 연출):** 원본 풀사이즈 그대로 사용 — 이 순간만 크게 보여줘서 임팩트 확보, 이후 인게임에서는 축소판으로 통일

### To-Do (코드 구현 시)
- [ ] `SPRITES.pets.sangsahwa`에 `drawH:44` 기준으로 `drawW`/`offsetX`/`offsetY` 계산해서 등록
- [ ] 해금 팝업(`_s2UnlockMsg` 표시부)에 원본 풀사이즈 별도 렌더링 분기 추가

---

## 19. 동료 파편 시스템 — 5각성 캡 이후 잉여 파편의 용도 확정 (설계 확정, 코드 미반영)

### 발견
동료 성장 구조는 파편(5개=별1개) → 별(5개=각성1단계, 별 리셋) → **각성 최대 5단계(5각) 캡**.
5각성 도달 후에는 `_processStars()`의 `while(frags>=5 && awk<5)` 조건 때문에 파편이 더 이상
별/각성으로 전환되지 않고, `companionFragments[id]`에 숫자만 계속 쌓이는 **죽은 재화** 상태가 됨.
(코드 수정 없이도 이미 자동으로 누적되고 있음 — 별도 저장 로직 추가 불필요)

### 확정 방향 — 시즌6(원계) "법칙" 재화로 전환
기존 세계관 문서 미결사항이었던 "시즌6 법칙 아티팩트가 실용 아이템인지 서사적 수집품인지"를
이걸로 해결: **5각성 캡을 찍은 동료의 잉여 파편을 시즌6 해금 후 법칙 구매/강화 재화로 전환**하는
실용 아이템 트랙으로 확정.

- 동료들과 쌓아온 인연(파편)이 우주의 법칙을 다루는 힘으로 승화된다는 서사적 연결도 자연스러움
  (애기씨 여정 아크 "선계를 돕는다 → 원계에서 법칙과 마주한다" 흐름과 맞물림)
- 교환비/UI 상세는 시즌6 실제 설계 시점에 확정 필요 (예: 동료별 잉여 파편 10개 = 법칙 정수 1개,
  또는 만능파편처럼 동료 구분 없이 통합 전환 — 두 방식 중 미정)

### To-Do (시즌6 설계 시점에)
- [ ] 파편→법칙 재화 교환비 확정
- [ ] 동료별 개별 전환 vs 통합 전환 방식 결정
- [ ] 법칙 구매/강화 UI 설계 (원계/표방원계 전용 화면)
- [ ] 세계관 문서(`WORLDBUILDING_v0.5.md`) 시즌6 섹션에 "법칙=실용 아이템" 확정사항 반영 완료

### 배경
기존 보스러시는 챕터1~5 보스 시퀀스 10마리 고정(`BOSS_RUSH_SEQ`), 전부 처치해야 클리어 처리되는 구조.
이걸 "지금까지 클리어한 보스 전부가 무한히 랜덤 등장 + 갈수록 강해지는" 방식으로 전면 재설계.

### 확정 설계

**보스 풀**
- 현재까지 클리어한 챕터들의 (미들보스 + 챕터보스) 전부, 중복 허용 랜덤 추첨
- 예: 7챕터 2스테이지 진행 중이면 1~6챕터 보스 풀(12종)에서 매번 랜덤 뽑기 — 같은 챕터 보스가 연속으로 나올 수 있음

**난이도 복리 스케일링**
- i번째(0-indexed) 스폰 보스 스탯 배율 = `1.05^i`
- 10번째 보스(i=9) ≈ ×1.55 / 20번째 보스(i=19) ≈ ×2.53

**보상 스케일링**
- 확정 보석(💎): i번째 처치 = `round(1 × 1.05^i)` — 보스 강함과 함께 복리 증가
- 빅골드 드랍: 기존 그대로 **고정 1~2개** (스케일링 안 함, 보석 전환분도 고정치)

**종료 조건**
- 기존 "10마리 시퀀스 전부 소환+처치" 방식 폐기
- 무한던전(웨이브)과 동일하게 **플레이어 사망 시 종료**, 처치 수 기록(`bossRushRecord`처럼 최고 기록 갱신 방식 유지)

**확정 보석 시작값 및 누적 (base=8)**

기존 base=1 대신 처치당 시작 보상을 **8개**로 상향 확정 (난이도 배율 1.05는 그대로 유지, 보상과 난이도 배율을 분리 — 배율 자체를 올리면 후반 보스 스탯이 과도하게 폭증하므로 시작값만 조정):

```
i번째(0-indexed) 처치 보상 = round(8 × 1.05^i)
```

| 처치 수 | 누적 보석 |
|---|---|
| 10마리 | 100개 |
| 15마리 | ≈173개 |
| 20마리 | ≈265개 |
| 30마리 | ≈531개 |

> ⚠️ 기존 보스러시(고정10마리, base=1) 대비 대폭 상향된 수치. 다이아 경제 전반(업적 보상 1~10개대,
> 상점 교환비 등)과 비교했을 때 무한모드가 다이아 메인 소스로 확실히 자리잡는 방향인지 실플레이
> 체감 확인 후 조정 필요.

### To-Do (코드 구현 시)
- [ ] `BOSS_RUSH_SEQ` 고정 배열 방식 폐기, 클리어한 챕터 기반 동적 풀 생성 함수로 교체
- [ ] 보스 스폰 시 `1.05^i` 스탯 배율 적용 (i = 누적 스폰 인덱스)
- [ ] 처치 보상 💎 계산도 동일 배율(`round(8*1.05^i)`) 적용, 빅골드는 기존 고정 로직 유지
- [ ] 종료 조건을 "시퀀스 소진"에서 "플레이어 사망"으로 변경 (무한던전 종료 로직 재활용 가능한지 검토)
- [ ] 결과화면 타이틀 문구도 "N보스 처치" 형태로 무한던전과 통일할지 검토

---

## 16. 시즌2 엔딩씬 5슬라이드 확정 (이미지 완료, 코드 미반영)

### 확정 슬라이드 구성
기존 시즌1 엔딩 구조(`ending-scene.js` SLIDES 배열)를 5슬라이드로 확장.

| # | 장면 | 대사(초안) |
|---|---|---|
| 1 | 유명계 강, 저승나비 | "멈춰있던 넋들이, 다시 강을 따라 흐르기 시작했다." / "저승나비가 그 위를 조용히 맴돈다." |
| 2 | 바리공주, 상사화 전달 | "타락에서 돌아온 바리공주가 상사화 한 송이를 건넨다." / ""이 꽃처럼... 나도 누군가를 다시 만날 수 있을까."" |
| 3 | 균열(단독 눈) | "그러나 어딘가에서, 실눈 하나가 천천히 떠졌다." |
| 4 | 균열+숲속 눈들 | "숲 곳곳에서 낄낄거리는 기척이 느껴진다." / "누군가... 지켜보고 있다." |
| 5 | 여명+도깨비 실루엣(나뭇가지) | "먼동이 트고 있었다." / ""망랑계의 문이... 열리고 있어."" |

- 이미지 5장 전부 제작 완료(제미나이 생성, 워터마크 크롭 필요 — 390×844 기준 여유분 있게 뽑은 뒤 하단 크롭)
- 도깨비 2마리 클로즈업 버전(방금 추가 생성분)은 엔딩 대신 **시즌3 인트로 슬라이드 1번째 장 후보** — 이유: 여명(밝아오는 하늘) 없이 완전한 밤 상태라 엔딩의 "희망" 톤과 안 맞고, 코믹한 도깨비 얼굴이 다음 시즌 오프닝 훅으로 더 어울림. 실제 인트로에 넣어보고 안 맞으면 다른 이미지로 교체

### To-Do (코드 구현 시)
- [ ] `EndingScene`의 `SLIDES` 배열을 2개→5개로 확장 (season2 전용 별도 배열 `SLIDES_S2` 신설 권장)
- [ ] `_pendingEnding2` 플래그 추가 — `stageId===200 && difficulty==='easy' && victory && !saveData.season2ClearEnding` 최초 1회 트리거
- [ ] `SPRITES.ending`에 시즌2용 5장 등록 (기존 시즌1용 0,1 인덱스와 겹치지 않게 네임스페이스 분리 필요 — 예: `SPRITES.endingS2[0~4]`)
- [ ] 이미지 5장 워터마크 크롭 후 최종 반입
- [ ] 도깨비 클로즈업 이미지는 `IntroScene`(시즌3용 신규 인트로) 첫 슬라이드 후보로 배치, 실제 붙여보고 톤 안 맞으면 교체

---

## 17. 무기초월 이미지 — 이미 준비 완료 확인 (이미지 목록에서 제외)

기존 260706 문서에서 확인된 대로 `soul_wp_1~5.png`(부적/신검/신궁/지팡이/영혼낫 9~10성 최종형태) **5종 전부 PC에 준비 완료** 상태. 이미지 작업이 아니라 **코드 등록만 남음** — 10번(시즌3 이미지 목록)이나 별도 이미지 대기 목록에 다시 올릴 필요 없음.

### To-Do (코드 구현 시)
- [ ] `SPRITES.weapons`에 9~10성 전용 키 추가 + `blacksmith-scene.js`/`weapons.js`에서 `transcendLv>=9`일 때 해당 이미지로 교체하는 분기 연결
- [ ] glow 강도 파라미터화(1~3성 vs 4~8성 vs 9~10성)와 함께 처리

---

## 21. 신규 서브무기 — 소금 뿌리기(정화의 소금) (설계 확정, 코드 미반영)

### 배경
itch.io 유저 피드백: 정화수(성수) 관련 글에 "소금도 정화·퇴마 효과로 문화권 공통 인식이 있다"는 제안 →
서브무기 신규 추가로 확정.

### 컨셉 — 파티클 흩뿌림 + 짧은 경직 (area 카테고리, CC 혼합)
성수웅덩이(`holy_water`, 고정 웅덩이형)와 겹치지 않게 **"애기씨 주변 즉발 파티클 방사"**로 차별화.
불꽃놀이처럼 매우 작은 흰색 발광 입자가 방사형으로 흩어지며, 맞은 적마다 개별 판정으로 약한 데미지 +
짧은 경직(스턴).

```js
salt_scatter: {
  id:'salt_scatter', name:'정화의 소금', icon:'🧂', category:'area', rarity:'common',
  desc:'소금을 사방에 흩뿌려 적을 맞히고, 맞은 적을 잠시 멈춘다.',
  maxLevel:4,
  levels:[
    {cooldown:3.5, particleCount:12, particleDmg:3, stunDur:0.3,  scatterRadius:90 },
    {cooldown:3.2, particleCount:15, particleDmg:4, stunDur:0.35, scatterRadius:100},
    {cooldown:2.9, particleCount:18, particleDmg:5, stunDur:0.4,  scatterRadius:110},
    {cooldown:2.6, particleCount:20, particleDmg:6, stunDur:0.45, scatterRadius:120},
  ],
}
```

- 파티클 개수만큼 랜덤 각도로 개별 투사체 발사, 맞은 적마다 개별 데미지+경직 판정 (면적 daño가 아니라
  다수 개별 히트 방식이라 부적 회전형과도 결이 다름)
- 데미지는 낮게(레벨당 3~6), 경직도 짧게(0.3~0.45초) — 메인 딜이 아니라 견제+CC용 포지션으로
  debuff 카테고리(저승낫/얼음부적)의 강한 CC와 차별화

### 이미지 필요 여부 — 인게임 파티클은 불필요, 카드 아이콘만 필요

**인게임 파티클(발광 소금 알갱이): 이미지 불필요, 코드로 처리**
기존 XP오브/골드/진화이펙트(`spawnEvolveEffect`)와 동일 패턴 — radial gradient 발광 + 작은 원.
```js
function drawSaltParticle(ctx, sx, sy, alpha) {
  ctx.save();
  ctx.globalAlpha = alpha;
  const g = ctx.createRadialGradient(sx, sy, 0, sx, sy, 6);
  g.addColorStop(0, 'rgba(255,255,255,0.9)');
  g.addColorStop(1, 'transparent');
  ctx.fillStyle = g;
  ctx.beginPath(); ctx.arc(sx, sy, 6, 0, Math.PI*2); ctx.fill();
  ctx.fillStyle = '#ffffff';
  ctx.beginPath(); ctx.arc(sx, sy, 1.5, 0, Math.PI*2); ctx.fill();
  ctx.restore();
}
```

**레벨업 카드 아이콘: 이미지 필요** (`CARD_IMGS`, 기존 원칙대로 `EFFECT_IMGS`와 완전 분리)

공통 스타일 고정 문구:
```
16-bit pixel art, top-down/isometric game item icon,
Korean folk aesthetic, transparent background,
clean pixel outline, soft white ambient glow,
game asset style consistent with Vampire Survivors-like mobile RPG
```

아이템 디테일:
```
a small wooden bowl of coarse white salt with a pinch of salt
being scattered above it, tiny sparkling white grains suspended
in the air, subtle soft white glow, humble and simple design,
common-tier item rarity
```

### To-Do (코드 구현 시)
- [ ] `game-data.js`(또는 서브무기 정의 파일)에 `salt_scatter` 엔트리 추가
- [ ] 파티클 스폰/개별 판정/경직(stun) 로직 신규 구현 (진화이펙트 파티클 스폰 패턴 참고)
- [ ] `CARD_IMGS`에 카드 아이콘 1장 등록 (이미지 작업 단계에서 제작)
- [ ] 레벨업 선택지 풀(`getLevelUpChoices`의 area 카테고리)에 편입

---

## 20. 유저 피드백 대응 — 줌 발견성 + 파밍타임 자석 (설계 확정, 코드 미반영)

### 배경
itch.io 유저 피드백: 부적(초반 무기) 사용 중 화면 확대/축소 기능을 몰라서 화면 밖에서 죽는 몹의
경험치를 못 먹었고, 화면을 줄여도 파밍 시간 내에 왕복이 안 돼서 접었다는 신고.

원인 확인:
- 튜토리얼 4슬라이드(이동/무기/동료/건물) 어디에도 줌 관련 안내 없음 — 핀치줌/마우스휠줌/우하단
  +/− 버튼이 전부 코드엔 있지만 존재를 알려주는 문구가 전무
- 파밍 구간(`state==='farming'`) 자석 범위가 평소와 동일해서, 화면을 넓혀도 왕복 이동 시간이
  파밍 타임보다 길면 사실상 못 먹는 문제

### 확정 수정안

**① 스테이지1 최초 입장 튜토 연계**
- 기존 인트로 슬라이드(4장) 종료 직후 → 스테이지1 최초 진입 시(1회성 플래그로 추적)만 트리거
- 기존 스테이지 인트로 텍스트(챕터명/스테이지명, 1.5초 노출)와 자연스럽게 이어지는 흐름으로 배치

**② 줌 버튼 컨텍스트 스팟라이트**
- 우하단 줌 +/− 버튼 주위에 테두리 박스 하이라이트, 몇 초간 표시 후 자동 페이드아웃
- 논블로킹(팝업 아님, 게임 진행 막지 않음), "두 손가락으로도 확대/축소할 수 있어요" 짧은 말풍선 동반
- 스테이지1 최초 진입 시에만 노출 (①과 세트)

**③ 파밍타임 자석 범위 ×3**
- `state==='farming'`(킬타겟 달성~스테이지 종료 전 파밍 구간) 동안 플레이어 자석(픽업) 범위(`player.magnetRange`) 임시 ×3 적용
- 파밍 종료 시 원래 값으로 복구

### To-Do (코드 구현 시)
- [ ] `saveData.stage1TutorialSeen` 플래그 추가, 스테이지1 최초 클리어(또는 최초 진입) 시 1회 트리거
- [ ] 줌 버튼 좌표 기준 하이라이트 오버레이 UI 구현 (canvas 오버레이 또는 DOM 레이어)
- [ ] `state==='farming'` 진입 시 `player.magnetRange`를 임시로 ×3 배율 적용(원본 값 별도 저장), farming 종료/게임 재진입 시 원복
- [ ] 기존 튜토리얼 lang.js 슬라이드1 텍스트에도 줌 언급 추가 검토(②와 중복 안내라도 텍스트 튜토 완주하는 유저 대비 안전망)

### ⚠️ 재검토 결과 — 파밍타임 레벨업 카드 미노출은 버그 아님 (정정)

처음엔 `if(o.dead && player.gainXp)`가 리턴값을 체크 안 해서 `triggerLevelUp()`이 안 불린다는 걸
버그로 판단했으나, 재검토 결과 **의도상 문제없는 동작**으로 정정:

- 인게임 카드로 오르는 무기강화/각성치는 **그 판(런) 한정 임시 파워**이며 대장간 영구강화
  (`saveData.weaponLevels`)와 완전히 분리된 시스템 — 런 종료 시 초기화, 저장/이월 안 됨
- 파밍타임은 이미 몬스터 스폰이 끝나고 5초 뒤 `endGame()`으로 결과화면 이동하는 구간이라,
  카드를 띄워도 그 파워를 쓸 전투 자체가 없음
- 따라서 파밍타임엔 XP는 정상 흡수되고 레벨/자동 스탯 증가는 조용히 적용되되, 카드 UI를
  생략하는 지금 동작이 오히려 불필요한 인터럽트를 막아주는 합리적인 부작용 없는 동작
- **코드 수정 불필요** — 이 항목은 To-Do에서 제외
