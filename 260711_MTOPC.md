# 260711 모바일 → PC 핸드오프

세션: 모바일(Claude.ai)
주제: 오행(五行) 시스템 확정 (동료 오행 배정 + 무기/동료/펫 시너지 설계) + 슬롯 해금 로직 확인

---

## 1. 동료 리스킨 + 오행 배정 (설계 확정, 코드 미반영)

### 이름 변경 (9번 항목 리스킨과 연계, 신규 확정 3건)

| 기존 | → 변경 |
|---|---|
| 검객 도치 | **장승 지킴이** |
| 철갑 수호신 | **수호신** |
| 천계 역사 금강 | **생령 역사** |

### 동료 13종 오행 최종 배정

| 오행 | 동료 | 근거 |
|---|---|---|
| 金 | 백호신, 신검 무사, 강림차사 | 백호=서방수호신(사신 정합) / 검 / "영혼을 거두는" 행위가 金의 원전 상징(숙살지기, 가을=거둠=심판)과 일치 |
| 木 | 아람(궁수), 생령 역사(구 금강) | 활 / 색상(적)보다 이름·서사 기준 우선, 색상 정합성은 보류 |
| 火 | 꺽쇠(폭탄), 소희(봉황), 해원맥 | 화염폭탄·불새 / 유지 |
| 土 | 장승 지킴이(구 도치), 수호신(구 철갑수호신) | 땅에 박힌 마을 수호상 / "대지처럼 버티는 최후 방어선" 컨셉 |
| 水 | 단비(무당), 청아(용왕녀), 가온(암살자) | 치유·정화 / 능력(빙결) 우선(이름 용=木과 충돌하는 대표 사례) / 은신·그림자는 水의 藏(장) 속성 |

**분포 3-2-3-2-3** — 기존 초안 대비 金 쏠림(5종) 완전 해소.

### ⚠️ 색상 보정 관련 메모 (이미지 작업 단계용)
- 생령 역사(구 금강)는 기존 스프라이트가 적/주황 계열(火 이미지)인데 오행은 木으로 확정 → 실제 이미지 작업 시 hue-shift(색조 회전)로 청록/녹색 계열로 보정 예정
- 기존 "스마트 이미지 커터 앤 배경제거 프로그램"에 이미 있는 hue-shift 로직 재사용 가능할 것으로 예상 (시즌2 바닥 텍스처 갈색→보라 보정과 동일 방식)

### 동료 13종 등급 재배치 (신규 7단계 사다리 적용 확정)

기존 4단계(common/rare/epic/special)를 펫(4번 참고)과 동일한 7단계 사다리로 통일.

| 등급 | 동료 |
|---|---|
| 커먼 | 꺽쇠 |
| 언커먼 | 장승 지킴이(구 도치), 아람, 단비, 가온, 수호신(구 철갑수호신), 신검 무사 |
| 레어 | 해원맥, 청아, 소희 |
| 유니크 | 생령 역사(구 금강), 백호신, 강림차사 |
| 에픽/레전더리/미소스 | (비어있음, 추후 콘텐츠용) |

**분포 1-6-3-3** (총 13종)

### To-Do (코드 구현 시, 1번 전체)
- [ ] `game-data.js`의 companions 배열 전체 `rarity` 필드 재배치 (위 표 기준)
- [ ] 뽑기 풀(`shop-scene.js`의 `GOLD_FULL_WEIGHTS`/`DIAMOND_FULL_WEIGHTS`) 확률 가중치를 신규 등급 기준으로 재조정
- [ ] 동료 해금/강화 비용도 펫과 동일한 7단계 비용표(4-5 참고) 적용할지 별도 비용표를 새로 짤지 결정

### 동료 뽑기(정성굿/대신굿) 확률 가중치 확정

펫은 가챠 없이 직접구매(용왕 연못)라 확률표 대상 아님 — 동료 뽑기 전용.

기존 "정성굿(골드)=명, 대신굿(다이아)=운" 2단 구조 유지, 등급 경계는 **레어를 양쪽 풀에 겹쳐서** 포함(예전 구조 계승).

| 굿 | 포함 등급 |
|---|---|
| 정성굿(골드) | 커먼 · 언커먼 · 레어 |
| 대신굿(다이아) | 레어 · 유니크 (+향후 에픽/레전더리/미소스 추가 예정) |

**가중치 (각 풀 합 100 기준)**

정성굿:
- 커먼(꺽쇠) 40
- 언커먼 6종 각 8 (합 48)
- 레어 3종 각 4 (합 12)

대신굿:
- 레어 3종 각 10 (합 30)
- 유니크 3종 각 20 (합 60)

### To-Do (코드 구현 시, 뽑기 가중치)
- [ ] `shop-scene.js`의 `GOLD_FULL_WEIGHTS`/`DIAMOND_FULL_WEIGHTS`를 위 표 기준 수치로 교체

---

## 2. 무기·동료·펫 오행 시너지 — 전 레이어 확장 설계 (설계 확정, 코드 미반영)

### 배경
기존엔 무기 5종에만 오행 상생/상극이 있었음. 동료·펫도 동일하게 오행을 가지므로, 3개 레이어(무기/동료/펫) 각각 독립적인 상생망을 갖고 + 슬롯 번호가 일치할 때 크로스 보너스가 얹히는 구조로 확장.

### 발동 방식 확정
- **패시브 상시 발동** (특정 트리거 없음, 장착만 하면 즉시 적용)
- 수치 강도는 **무기초월 8성 서브메커닉보다 약하게** 설계 (여러 시너지 중첩 고려)

### 2-1. 레이어별 상생 시너지

**무기 레이어 (기존 컨셉에 수치 확정)**

| 상생 조합 | 오행 관계 | 효과 |
|---|---|---|
| 지팡이+신궁 | 水生木 | 신궁 화살 사거리 +15% |
| 신궁+부적 | 木生火 | 화살 적중 시 화상 DoT: 데미지의 10%, 2초간 |
| 부적+영혼낫 | 火生土 | 영혼낫 지속시간 +15% |
| 영혼낫+신검 | 土生金 | 낫 처치마다 신검 쿨타임 -3%, 최대 5스택(-15%), 8초 미처치 시 스택 초기화 |
| 신검+지팡이 | 金生水 | 신검 크리티컬 시 20% 확률로 지팡이 오브 1개 임시 추가발사(3초) |

- 하드(3슬롯) 상생 연쇄 2단 완성 시: 참여 3무기 **쿨타임 -5%** 추가

**동료 레이어 (신규)**
- 2개 상생: 동료 공격력 +6%
- 하드 3슬롯 연쇄 완성: 동료 전체 쿨타임 -4% 추가

**펫 레이어 (신규)**
- 2개 상생: 펫 효과량 +6%
- 하드 3슬롯 연쇄 완성: 펫 효과량 +4% 추가 (합 10%)

### 2-2. 무기 상극 (리스크&리워드, 기존 컨셉에 수치 확정)

5조합 전부 동일 수치로 통일(개별 효과 없음, 심플하게):
- 상극 조합 장착 시 두 무기 **데미지 +8%**, **받는 피해 +5%**

| 조합 | 오행 관계 |
|---|---|
| 신궁↔영혼낫 | 木克土 |
| 영혼낫↔지팡이 | 土克水 |
| 지팡이↔부적 | 水克火 |
| 부적↔신검 | 火克金 |
| 신검↔신궁 | 金克木 |

### 2-3. 슬롯 매칭 크로스 시너지 (신규 핵심 설계)

무기/동료/펫은 각각 독립적으로 슬롯 1/2/3을 가짐(난이도에 따라 1~3개). **같은 슬롯 번호에서 오행이 일치할 때** 추가 보너스:

| 단계 | 조건 | 보너스 |
|---|---|---|
| 2종 일치 | 같은 슬롯번호에서 동료+펫 오행 일치 | 해당 슬롯 동료 효과 +8% |
| 3종 일치(삼위일체) | 같은 슬롯번호에서 무기+동료+펫 오행 전부 일치 | 해당 오행 무기 데미지 +12% + 오행별 flavor 이펙트(2-4 참고) |

- 하드 3슬롯이면 슬롯 1/2/3 각각 독립 체크 → 최대 3중첩 가능 ("완전 오행 정렬 빌드"가 엔드게임 목표로 기능)

### 2-4. 삼위일체 flavor 이펙트 확정

공통 데미지+12% 위에 오행별 개성 이펙트 추가:

| 오행 | Flavor 이펙트 | 수치 | 근거 |
|---|---|---|---|
| 金 | 크리티컬 확률 | +8% | 예리함·베어내는 정확성 |
| 木 | 흡혈 효율 증폭 | 기존 `_vampire`(신목 흡혈 스탯) × 1.25 | 흡혈 투자자에게만 실이득 — 생명력 상징과 자연스럽게 연결, 흡혈 미투자 유저는 무효과이므로 木 라인이 흡혈 빌드와 궁합을 이루도록 유도 |
| 火 | 공격속도(쿨타임 감소) | -8% | 타오르는 속도감 |
| 水 | 받는 피해 감소 | -10% | 흐르며 받아넘기는 유연함 |
| 土 | 최대 HP | +10% | 대지처럼 굳건한 생명력 |

- 金·火 = 공격형, 水·土 = 생존형으로 자연스럽게 갈림 (木은 조건부 유틸)

### 2-5. 삼위일체 시각 이펙트 (코드 구현 가능, 신규 아트 불필요)

삼위일체 달성 슬롯 수만큼 캐릭터 주변에 발광 구체가 궤도를 도는 연출. 무기 `staff`(지팡이)의 오브 궤도 렌더링 로직 재사용 가능 — 단색 원 + glow(box-shadow 또는 canvas radial gradient)만으로 구현, 별도 스프라이트 불필요.

**스펙**
- 캐릭터 크기 기준(32px) 궤도 반경 **50px**
- 발광체 반지름 **15px**
- 회전 속도 **5초/바퀴** (느린 궤도)
- 개수 = 삼위일체 달성 슬롯 수 (1~3개, 하드 3슬롯 전부 달성 시 3개가 360°/3 간격으로 동시 회전)
- 색상 = 해당 슬롯에서 발동한 오행 색상 (金=백/木=청/火=적/土=황/水=흑) — 슬롯마다 오행이 다르면 구체 색도 각각 다르게
- 슬롯 조합이 깨져 삼위일체가 해제되면 해당 구체도 즉시 사라짐 (상시 재계산)

### 2-6. 오행 표기 UI (무기/동료/펫 아이콘)

한글/영문 텍스트 대신 **한자 1글자 + 색상 배지**로 표기 (언어 중립적, `lang.js` 이중관리 불필요).

- 무기/동료/펫 아이콘 우측 상단(또는 좌하단)에 작은 원형 배지
- 배지 = 오행 색상 배경 + 한자 1글자(金/木/水/火/土)
  - 金=백 배경/검은 글씨, 木=청 배경/흰 글씨, 火=적 배경/흰 글씨, 水=흑(또는 남색) 배경/흰 글씨, 土=황 배경/검은 글씨
- 기존 각성 등급 배지(★ 표시) 위치 문법 재사용 가능 (카드/캐릭터 화면에 이미 우상단 배지 쓰는 패턴 존재)
- 색상만이 아니라 한자도 함께 표기해 색맹 접근성 확보

### To-Do (코드 구현 시)
- [ ] 무기/동료/펫 오행 데이터 필드 추가 (`element: 'fire'|'water'|'wood'|'metal'|'earth'` 등)
- [ ] 슬롯별 오행 일치 판정 로직 (슬롯 인덱스 기준 비교)
- [ ] 오행 배지 UI 컴포넌트 구현 (2-6 스펙 기준)
- [ ] 삼위일체 궤도 이펙트 구현 (staff 오브 렌더링 로직 참고, 2-5 스펙 기준)
- [ ] 5종 상생/상극 효과 함수 구현 (기존 `computeWeaponGrowth()` 패턴 참고 가능할지 검토)
- [ ] 밸런스 검증: 하드 3중첩 시너지가 전체 파워 스케일링과 충돌하지 않는지 확인 필요

---

## 3. 슬롯 해금 로직 — 코드 확인 결과 (기존 구현 재확인, 오해 정정)

모바일 세션에서 "슬롯 해금이 신규 시스템"이라고 잘못 판단했으나, 실제로는 **이미 구현되어 있음**을 코드에서 확인함.

### 기존 구현 (game.js)
```js
if (difficulty === 'normal' && !saveData.clearedStagesNormal.includes(stageId)) {
  saveData.clearedStagesNormal.push(stageId);
  if (saveData.clearedStagesNormal.length === 1) saveData._showSlotUnlock = 'normal';
}
if (difficulty === 'hard' && !saveData.clearedStagesHard.includes(stageId)) {
  saveData.clearedStagesHard.push(stageId);
  if (saveData.clearedStagesHard.length === 1) saveData._showSlotUnlock = 'hard';
}
```
- 노말/하드 각각 **첫 스테이지 클리어 시점**에 슬롯 해금 팝업 트리거 (`lang.js`에 `slotUnlockNormal`/`slotUnlockHard` 텍스트 존재)
- 즉, "노말 1챕터 1스테이지 클리어→2슬롯", "하드 1챕터 1스테이지 클리어→3슬롯"은 **이미 구현 완료 상태**

### ⚠️ 미구현으로 확인된 부분 — 이지 슬롯 확장
- `DIFF_CONFIG`(stage-select.js)에 `slotMain:1`로 고정값만 있고, 이지 슬롯을 2/3으로 늘리는 동적 해금 로직은 코드에서 발견되지 않음
- **신규 구현 필요**: 챕터5 클리어 → 이지 2슬롯 / 시즌1 전체 클리어 → 이지 3슬롯

### To-Do (코드 구현 시)
- [ ] `saveData` 플래그 추가: `easySlot2Unlocked`(챕터5 클리어 시), `easySlot3Unlocked`(시즌1 클리어 시)
- [ ] `DIFF_CONFIG.easy.slotMain` 등을 고정값 대신 `saveData` 기반 동적 계산으로 변경
- [ ] 기존 `_showSlotUnlock` 팝업 패턴 재사용해서 이지 확장 시에도 동일 UX 적용
- [ ] `lang.js`에 이지 슬롯 해금 전용 텍스트 추가 검토 (기존 `slotUnlockNormal/Hard`와 구분 필요한지)
- [ ] `DIFF_CONFIG.easy.slotMain` 등을 고정값 대신 `saveData` 기반 동적 계산으로 변경
- [ ] 기존 `_showSlotUnlock` 팝업 패턴 재사용해서 이지 확장 시에도 동일 UX 적용
- [ ] `lang.js`에 이지 슬롯 해금 전용 텍스트 추가 검토 (기존 `slotUnlockNormal/Hard`와 구분 필요한지)

---

## 4. 펫 시스템 — 강화 로직 미구현 발견 + 효과 6종 재설계 + 등급 체계 전면 개편 (설계 확정, 코드 미반영)

### 4-1. ⚠️ 중요 발견 — 펫 강화가 실제로는 아무 효과 없음

`pet.js`의 `upgradePet()`을 코드로 확인한 결과, 골드+천령과를 소모해서 `petLevels[id]`를 1→5까지 올리는 UI/저장구조는 있지만, **`applyPetPassives()`가 `pd.value`(게임데이터에 고정된 값)를 그대로 사용하고 레벨을 전혀 참조하지 않음**. 즉 현재 펫 "강화"는 돈만 쓰고 회복량/크리티컬 등 실제 수치는 전혀 안 오르는 상태 — 버그라기보다 **미구현 상태**로 판단됨.

- [ ] **신규 구현 필요**: `petLevels[id]`(1~5)에 따라 `pd.value`를 스케일링하는 공식 추가 (다음 세션에서 레벨당 증가폭 수치 설계 예정)

### 4-2. 펫 효과 22종 현황 정리 (기존 파악)

기존 8종 + 십이지신 12종 + 시즌2 2종 = 22종. 효과 타입 9종(defense/crit/xp_boost/cooldown/magnet/regen/confuse/knockback/mark) + 시즌2 전용 2종(soulBoost/autoRevive)에 중복 다수 확인.

### 4-3. 십이지신 6종 — 신규 효과로 전면 교체 (기존 효과와 중복 해소 겸용)

| 펫 | 기존 효과 | → 신규 효과 | 설계 |
|---|---|---|---|
| 미신(양) | regen (거북령과 중복) | **골드 부스터** | 기존 서브무기(호패류)에 이미 있는 `player._goldMult` 필드 재사용 |
| 해신(돼지) | regen (거북령과 중복) | **던전 재화 부스터** | 신규 필드 `player._specialMult` — 특화 던전(강화석/천운석/천령과/태극석/차원석) 드랍량만 증가, 일반 스테이지 무효 |
| 술신(개) | defense (호야와 중복) | **자동 수집기** | 신규 AI 로직 — 펫이 플레이어 반경 밖 놓친 골드/XP 오브를 스스로 찾아가 흡수. 초기 이동속도 느림, `petLevels` 레벨업 시 속도 증가 (4-1 강화시스템 구현의 첫 실사용 사례로 연결) |
| 유신(닭) | xp_boost (터주신과 중복) | **동료 공격력 업** | 신규 필드 `player._compAtkMult`, `CompanionEntity` 대상 적용 로직 신규 필요 |
| 자신(쥐) | xp_boost (터주신과 중복) | **동료 방어력 업** | 신규 필드 `player._compDefMult` |
| 축신(소) | defense (호야와 중복) | **동료 HP 업** | 신규 필드 `player._compHpMult` |

⚠️ 유신/자신/축신 3종은 `CompanionEntity`에 스탯 곱연산 적용부가 신규로 필요 (현재 `applyPetPassives()`는 `player`만 대상으로 함).

### 4-4. 잔여 중복 5쌍 → "약캐/강캐" 페어링으로 재해석 (컷 대신 등급 분리로 해결)

기존 계획은 "중복이니 컷"이었으나, 오늘 세션에서 **"동일 효과의 하위/상위 등급 버전"으로 재정의**하기로 결정 — 22종 전부 유지 가능.

| 효과 | 하위 버전 (커먼/언커먼) | 상위 버전 (레어/유니크) |
|---|---|---|
| crit | 여우령(커먼) — **15%** | 인신·범(유니크) — **25%** |
| cooldown | 묘신·토끼(언커먼) — **-15%** | 달토끼(레어) — **-22%** |
| knockback | 도깨비(커먼) — **100** | 진신·용(유니크) — **180** |
| mark | 까마귀삼신(언커먼) — 단일 대상 **+20%** 데미지 | 사신·뱀(레어) — 3명 동시 표식, 각 **+15%** 데미지 (한방 vs 광역으로 성격 차별화) |
| confuse | 총각신(커먼) — **1.5초** | 신신·원숭이(레어) — **2.5초** + 범위 소폭 확대 |

설계 기준: 등급 갭에 따라 대략 커먼=기준 → 언커먼 +15~20% → 레어 +30% → 유니크 +50% 수준 격차. mark만 예외로 "단일 고배율 vs 다중 저배율"로 메커닉 자체를 차별화(순수 수치差 대신 플레이스타일 선택지로 설계).

### 4-5. 등급 체계 전면 개편 (펫 + 향후 동료도 동일 사다리 적용 예정)

기존 4단계(common/rare/epic/legendary)를 **7단계**로 확장. 에픽/레전더리/미소스(신화)는 이번엔 배정 없이 **향후 콘텐츠용으로 비워둠** — 현재 22종 전부 커먼~유니크 4단계 안에 재배치.

**새 등급 사다리**: 커먼 < 언커먼(신규) < 레어 < 유니크(신규) < 에픽 < 레전더리 < 미소스(신규)

**해금 비용 (천령과)**

| 등급 | 비용 |
|---|---|
| 커먼 | 10 |
| 언커먼 | 30 |
| 레어 | 40 |
| 유니크 | 70 |
| 에픽 | 110 |
| 레전더리 | 180 |
| 미소스 | 290 |

**강화 비용 (레벨당, 골드=천령과 동일값)**

| 등급 | 비용 |
|---|---|
| 커먼 | 350 |
| 언커먼 | 1,000 |
| 레어 | 1,400 |
| 유니크 | 2,500 |
| 에픽 | 4,000 |
| 레전더리 | 6,000 |
| 미소스 | 10,000 |

### 4-6. 펫 22종 최종 등급 배치

| 등급 | 펫 |
|---|---|
| 커먼 | 거북령, 여우령, 도깨비, 총각신 |
| 언커먼 | 묘신(토끼), 까마귀삼신, 호야, 해신(돼지), 자신(쥐), 축신(소) |
| 레어 | 터주신, 달토끼, 사신(뱀), 신신(원숭이), 오신(말), 저승나비, 미신(양), 유신(닭) |
| 유니크 | 인신(범), 진신(용), 상사화, 술신(개) |
| 에픽/레전더리/미소스 | (비어있음, 추후 콘텐츠용) |

### 4-7. 펫 레벨 스케일링 공식 확정

**연속값(%, 수치형 효과)**: Lv1(기본값 100%) → Lv5(기본값 130%), 레벨당 +7.5%p 균등 증가

```js
function scalePetValue(baseValue, lv) {
  return baseValue * (1 + 0.30 * (lv - 1) / 4);
}
```

| 레벨 | 배율 |
|---|---|
| Lv1 | 100% |
| Lv2 | 107.5% |
| Lv3 | 115% |
| Lv4 | 122.5% |
| Lv5 | 130% |

적용 예: 여우령 crit 18%→23.4%(Lv5), 거북령 regen 8→10.4(Lv5), 터주신 xp_boost 30%→39%(Lv5) 등 대부분의 효과(%나 연속 수치)에 그대로 적용.

**정수형(개수) 효과 — 예외 처리**: 소수점 스케일링이 부자연스러운 "대상 수/횟수" 값은 Lv1~4 고정, **Lv5 도달 시에만 +1**

| 펫 | 항목 | Lv1~4 | Lv5 |
|---|---|---|---|
| 사신(뱀) | mark 대상 수 | 3명 | 4명 |
| 상사화 | 부활 횟수 | 1회 | 2회 |

※ 까마귀삼신처럼 mark라도 값이 데미지%(25%)인 경우는 정수 예외 대상 아니고 일반 연속값 공식 적용.

### 4-8. 펫 22종 오행 배정 (설계 확정)

십이지 12종은 전통 지지오행(地支五行) 그대로 적용, 나머지 10종(기본8+시즌2 2종)은 능력/테마 기준.

| 오행 | 펫 |
|---|---|
| 金 | 까마귀삼신, 도깨비, 호야, 신신(원숭이), 유신(닭) |
| 木 | 인신(범), 묘신(토끼), 상사화, 저승나비 |
| 火 | 여우령, 사신(뱀), 오신(말) |
| 土 | 축신(소), 진신(용), 미신(양), 술신(개), 터주신 |
| 水 | 자신(쥐), 해신(돼지), 거북령, 총각신, 달토끼 |

**분포 5-4-3-5-5** (총 22종)

- 십이지 12종 근거: 자=水/축=土/인=木/묘=木/진=土/사=火/오=火/미=土/신=金/유=金/술=土/해=水 (전통 지지오행 그대로)
- 저승나비는 원래 시즌2 배경(유명계=죽음) 우선으로 水 배정 검토했으나, 최종적으로 **나비의 변태(재생·부활) 상징**을 우선해 木으로 확정 — 상사화(재생/부활)와 같은 木 계열로 묶어 컨셉 일관성 확보

### To-Do (코드 구현 시, 4번 전체)
- [ ] `applyPetPassives()`에 4-7 스케일링 공식 반영 (연속값 공식 + 정수형 예외 분기 처리)
- [ ] 펫 오행 필드(`element`) `game-data.js`에 추가
- [ ] 신규 필드 3종(`_goldMult` 재사용, `_specialMult`, `_compAtkMult`/`_compDefMult`/`_compHpMult`) 구현
- [ ] 술신(개) 자동수집 AI 로직 신규 작성 (펫 엔티티 이동 패턴 변경 필요, 기존 궤도형과 다른 동작)
- [ ] `CompanionEntity`에 펫발 스탯 배율 적용부 신규 추가
- [ ] `pet.js`의 `UNLOCK_COST`/`UPGRADE_COST`를 신규 7단계 비용표로 교체
- [ ] `game-data.js`의 pets 배열 전체 `rarity` 필드 재배치 (4-6 표 기준)
- [ ] 동료 13종에도 동일 7단계 등급 체계 적용할지 여부 결정 (현재는 펫만 개편, 동료는 기존 common/rare/epic/special 유지 중)

---

## 5. 다음 세션 이어갈 것
- 이지 슬롯 확장 코드 구현 (3번 참고, 설계는 끝났으나 코드 미착수)
- 오행 시스템 전체 코드 구현 착수 (무기 초월 시스템, 영혼경제 밸런스 등 기존 우선순위 항목들과 순서 조율 필요)
