# 260716 모바일 → PC 핸드오프

세션: 모바일(Claude.ai) 설계/분석

---

## 1. 부적 무기 데미지 공식 (현재 코드 기준, 분석만 — 코드 미반영)

### 기본 공식 (`weapons.js` MAIN_WEAPON_DEFS.talisman.fire)

```js
dmg = Math.floor(
  lvl.damage
  × (player.totalAtk / 100)
  × (this._overAwkDmg || 1)
  × (this._transcendMult || 1)
)
```

| 요소 | 의미 |
|------|------|
| `lvl.damage` | 레벨별 기본 공격력 (아래 표) |
| `player.totalAtk/100` | 스탯 공격력 배율 (기본 100 = 1.0배) |
| `_overAwkDmg` | 5각 이후 초과각성 데미지 배율 (8번 각성카드 로직과 연동, 없으면 1) |
| `_transcendMult` | 무기 초월(10성) 데미지 배율 (없으면 1) |

### 레벨별 기본 수치

| Lv | 쿨타임 | damage | 관통 | 속도 |
|----|--------|--------|------|------|
| 1 | 1.2s | 12 | 0 | 280 |
| 2 | 1.0s | 16 | 0 | 300 |
| 3 | 1.0s | 18 | 0 | 300 |
| 4 | 0.85s | 22 | 1 | 320 |

### 발사 수 (Lv별 개수 증가형 — 신검과 다르게 낱개 데미지는 동일, 개수만 늘어남)
```
일반 부적 수 = lv + (awk>0 ? awk+1 : 0)
```
→ Lv별로 발사체 개수가 늘고, 개당 데미지는 `lvl.damage` 그대로 적용 (다단히트 아님, 발사체 각각이 독립 판정)

### 특강(타닥) — 메인 쿨타임 불변, 추가 발사체
- 특강 1~4단계마다 0.12초 후 부적 +1발 추가 발사
- 추가 발사체도 동일 `lvl.damage` 공식 적용 (별도 배율 없음)

### 각성 — 회오리 부적 (별도 데미지 라인)
```
회오리 데미지 = 원본(lvl.damage 기준) × 30%  (관통형 데미지 30% 고정 원칙 — BALANCE.md 공통 규칙)
```
- 1각: 회오리 1개(3초마다) / 2각: 2개(0.5초 간격) / 3각: 3개 / 4각: 4개
- 나선형 확장, 관통, 3초 유지

### 초월 8성 특전
```js
t8Pierce = (this._transcendRank||0) >= 8 ? 1 : 0
```
→ 초월 8성부터 전 발사체에 관통 +1 추가 적용

### 정리
부적의 실질 DPS는 "개당 데미지 × 발사 수 × (1/쿨타임)" + "회오리 라인(30% 딜 추가)" 조합. 개당 데미지 자체는 레벨 4단계 표에서 크게 안 늘고(12→22, 약 1.83배), 대신 **발사 수 증가와 각성 회오리 라인 추가로 총 DPS를 늘리는 구조** — 신검(개당 데미지 폭증형)이나 영혼낫(낫 개수+유지시간 복합형)과는 성장 축이 다름.

> 참고: 260707 핸드오프에서 5각 Lv4 기준 부적 이론 DPS는 259로, 5무기 중 최하위였음(낫 538, 신검 720). 발사 수 증가형 무기 특성상 "다수 처리엔 강하지만 단일 딜은 약함" 포지션으로 의도된 것으로 보이나, 재밸런싱 필요 여부는 실제 플레이 데이터로 판단 필요.

---

## 2. 무한던전 재화 스케일링 붕괴 + "던전강화" 프레스티지 시스템 (설계 확정, 코드 미반영)

### 발견된 문제 (`spawner.js`/`enemy.js`)

```js
// 무한던전 전용 배율 — 1만 킬마다 계단식 ×200
const dungeonMult = _isDungeon ? Math.max(1, Math.floor(kills/10000)*200) : 1;

this.hp        = Math.floor(t.hp * (1+wave*0.667) * dungeonMult);   // dungeonMult 적용
this.damage    = Math.floor(t.damage * (1+wave*0.18) * dungeonMult); // dungeonMult 적용
this.goldValue = Math.floor((t.gold||1) * (1+wave*0.10));            // ❌ dungeonMult 미적용
this.xpValue   = t.xp;                                               // ❌ 스케일링 자체 없음
```

**1만 킬 도달 순간 몬스터 HP/공격력이 계단식으로 ×200 급증하는데, 골드/XP 보상은 전혀 안 따라감.** 몬스터는 200배 세지고 보상은 그대로라, 1만킬 이후 무한던전 파밍 효율이 절벽처럼 떨어지는 구조. 캐릭터 창 기본 강화(공격력/방어력, 비용 ×2씩 증가)가 골드 대비 비효율적으로 느껴지는 근본 원인 중 하나 —애초에 1만킬 이후 골드 수급 자체가 벽에 부딪힘.

### ① 수정 방향 — 골드/재화만 dungeonMult 비례 적용 (XP는 제외 — 260716 확정)
```js
this.goldValue = Math.floor((t.gold||1) * (1+wave*0.10) * dungeonMult); // 골드: dungeonMult 적용
this.xpValue   = t.xp; // XP는 기존 그대로 유지 — 스케일링 안 함
```
**XP는 의도적으로 제외.** 몬스터가 세진 만큼 "돈이 되는" 보상(골드/재화)은 늘려야 리스크&리워드가 성립하지만, XP까지 같이 폭증하면 무한던전에서 레벨업 카드가 지나치게 자주 뜨면서 파밍 흐름(스킬 활용/이동)이 끊길 우려 있음. 골드·재화 스케일링만으로도 "위험한 만큼 번다"는 목적은 충분히 달성됨.

### ②③ 던전강화 (신설 — 건물강화 UI/구조 재사용)

새로운 영구강화 시스템. 골드+재화 소모로 레벨업하며, **레벨업 시 무한던전 시작 지점 자체가 밀림**:

```
던전강화 1강 = 이번 런은 "1,000킬 지점"부터 시작 (몬스터도, 골드/재화 배율도 그 지점 기준)
던전강화 2강 = "2,000킬 지점"부터 시작
...
던전강화 10강 = "10,000킬 지점"부터 시작 → dungeonMult ×200 상태로 런 시작
던전강화 11강 이상 = 11,000/12,000... 계속 이어짐 (다음 만킬 구간까지 자연 연장)
```

- UI/구조는 `building-scene.js`의 건물강화 패턴 그대로 재사용 (레벨별 비용 테이블 + 강화 버튼)
- 목적: 이미 강해진 유저가 "초반 약한 몬스터 구간"을 매번 반복해서 뚫고 올라가는 비효율을 없애고, 자기 실력에 맞는 난이도부터 바로 시작하게 해주는 프레스티지 장치

### ④ 균형 장치 (자연 발생)
던전강화를 과하게 올리면 런 시작부터 자기 전투력으로 감당 안 되는 몬스터를 만나게 됨 — "무조건 최대로 올리는 게 이득"이 아니라, 실제 딜/생존력에 맞춰 적정 레벨을 잡아야 하는 구조가 자연히 생김. 별도 페널티 시스템 불필요 — 몬스터 강함 자체가 리스크로 작동.

### ⑤ 종합 전투력 지수 (신규 UI)
캐릭터의 현재 장착 상태(주무기 레벨/각성/초월 + 보조무기 + 동료 별/각성 + 스탯강화 + 신목/명부강화 + 펫)를 종합해 숫자 하나로 환산하는 "전투력" 지수 도입.

- 던전강화 화면에서 "현재 던전강화 레벨 vs 내 전투력" 비교해서 **적정 / 위험 / 매우위험** 같은 안내 표시
- 정확한 계산 공식(가중치 배분)은 추후 별도 세션에서 설계 필요 — 이번엔 "이런 지표가 필요하다"는 방향만 확정

### ⑥ 덱 시스템 (신규 — 캐릭터 창)
캐릭터 창에 "무기셋" 버튼 추가 → **덱 1~5 슬롯**:
- 각 슬롯에 주무기 조합 + 보조무기 조합 + 동료 편성 + 펫 편성을 통째로 저장
- 버튼 한 번으로 저장된 조합 즉시 불러오기 (스테이지별/던전강화 레벨별로 다른 빌드를 빠르게 전환하는 용도)

### To-Do (PC 세션에서 설계 세부화 + 구현)
- [ ] `enemy.js`의 `goldValue`(및 재화 드랍 로직)에 `dungeonMult` 적용 — `xpValue`는 제외 (①)
- [ ] "던전강화" 데이터 구조/비용 테이블 설계 (`CONFIG.DUNGEON_UPGRADE` 형태로, `SINMOK` 패턴 참고)
- [ ] 던전강화 레벨 → 무한던전 시작 킬수 오프셋 적용 로직 (`Spawner.reset()`에 `startKills` 파라미터 추가)
- [ ] 던전강화 전용 화면 또는 `building-scene.js` 확장으로 UI 구현
- [ ] 종합 전투력 계산 공식 설계 (별도 세션 필요 — 가중치 미확정)
- [ ] 전투력 vs 던전강화 레벨 비교 UI (적정/위험/매우위험 라벨)
- [ ] 덱 시스템 데이터 구조 설계 (`saveData.decks[1~5] = { mainWeapons, subWeapons, companions, pets }`)
- [ ] 캐릭터 창에 "무기셋" 버튼 + 덱 저장/불러오기 UI 구현

---

## 3. 기본 강화에 체력(HP) 노출 추가 (UI 누락 수정, 코드 미반영)

### 발견 — 체력 강화는 이미 백엔드 완성 상태, UI만 안 붙어있음

```js
// config.js — 이미 존재
STAT_UPGRADE_PER_LEVEL: { atk:5, spd:3, mov:2, def:5, eva:1, hp:10 },

// player.js — 이미 존재, upg.hp 반영 로직 완성
if (upg.hp) {
  const bonus = upg.hp * upPerLv.hp;
  this.hp    += bonus;
  this.maxHp += bonus;
}
```

그런데 `player-scene.js`의 `STAT_INFO`엔 공격력/방어력만 있고 체력이 빠져있어서, 강화 버튼 자체가 화면에 안 뜸:
```js
const STAT_INFO = () => [
  { key:'atk', ... },
  { key:'def', ... },
  // hp 없음
];
```

### 배경
2번 항목(무한던전 1만킬 이후 200배 몬스터)에서 확인했듯, 이 시점부터는 공격력만으론 못 버티고 즉사 위험이 커짐. 체력 강화는 이미 로직이 다 있으니 **신규 개발이 아니라 UI 노출 누락 수정** — 가장 저비용으로 바로 체감 개선 가능한 항목.

### 수정 방향 (확정)
```js
const STAT_INFO = () => [
  { key:'atk', label:Lang.t('player','stat_atk').label, icon:'⚔️',  desc:Lang.t('player','stat_atk').desc, unit:'' },
  { key:'def', label:Lang.t('player','stat_def').label, icon:'🛡️', desc:Lang.t('player','stat_def').desc, unit:'' },
  { key:'hp',  label:Lang.t('player','stat_hp').label,  icon:'❤️', desc:Lang.t('player','stat_hp').desc,  unit:'' }, // 추가
];
```
`lang.js`의 `player` 섹션(한/영 둘 다)에 `stat_hp: { label:'체력', desc:'최대 체력 증가' }` 형태로 텍스트 추가 필요.

### To-Do (PC 세션에서 구현)
- [ ] `player-scene.js`의 `STAT_INFO`에 `hp` 항목 추가
- [ ] `lang.js`의 `player` 섹션(한/영)에 `stat_hp` 라벨/설명 텍스트 추가
- [ ] 강화 비용 곡선(`upgradeCost`)이 hp에도 동일하게 적용되는지 확인 (기존 `atk`/`def`와 같은 `saveData.statUpgrades` 구조 공유 여부 점검)
- [ ] 실기기에서 체력 강화 버튼 노출 + 강화 시 `maxHp` 실제 반영 확인

---

## 4. 버그: 레벨업 자동선택 중 수동 클릭 타이밍 겹치면 카드 2개 적용 (설계 확정, 코드 미반영)

### 증상
자동모드(`autoMode===2`)에서 레벨업 카드 3초 자동선택 대기 중, 수동으로 카드를 클릭하는 타이밍이 자동선택 시점과 겹치면 **카드 2개 효과가 한 번에 적용됨**.

### 원인 (`game.js`)
```js
function pickLevelUp(cat, idx){
  const c = ...;
  if(c){
    // 카드 효과 적용
    state='playing';
    return;
  }
  ...
}
```
`pickLevelUp()`에 **"이미 처리됐는지" 확인하는 가드가 없음**. 흐름:
1. 3초 자동선택 `setInterval`이 대기 중
2. 그 타이밍에 카드를 직접 클릭 → `pickLevelUp()` 1차 실행 (수동 카드 효과 적용, `state='playing'`)
3. 이미 예약돼 있던 `setInterval` 콜백은 그대로 실행됨 → `pickLevelUp()` 2차 실행 (자동 최적픽 카드 효과까지 추가 적용) — `state` 값과 무관하게 무조건 처리됨
4. 결과: 레벨업 1회에 카드 2장 몫 효과가 겹쳐 적용

### 수정 방향 (확정)
```js
function pickLevelUp(cat, idx){
  if (state !== 'levelup') return; // 추가 — 이미 처리된 레벨업 재실행 방지
  ...
}
```
추가로 수동 클릭 시 자동선택 타이머 자체를 정지시켜야 완전히 해소됨. 현재 `_iv`(setInterval 참조)가 `triggerLevelUp()` 내부 지역 스코프에 갇혀 있어 `pickLevelUp()`에서 접근 불가 — `window._levelupAutoIv` 같은 전역 참조로 빼서, 카드 클릭 시 `clearInterval(window._levelupAutoIv)` 호출 추가 필요.

### To-Do (PC 세션에서 구현)
- [ ] `pickLevelUp()` 최상단에 `if (state !== 'levelup') return;` 가드 추가
- [ ] `triggerLevelUp()`의 자동선택 `setInterval` 참조를 `window._levelupAutoIv`로 전역화
- [ ] `pickLevelUp()` 진입 시 `clearInterval(window._levelupAutoIv)` 호출 추가 (수동/자동 어느 경로로 호출되든 타이머 확실히 정지)
- [ ] 실기기에서 자동모드 켠 채로 3초 타이밍 맞춰 클릭 반복 테스트 → 카드 1장만 적용되는지 확인

