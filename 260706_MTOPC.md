# 260706 모바일 → PC 핸드오프

세션: 모바일(Claude.ai) / 대상 빌드: `mongdal-light.html` (경량, 이미지 제거본)
PC 멀티파일 구조 기준 대상 파일은 각 항목에 명시. **경량 빌드에는 이미 반영 완료**, PC 쪽에 동일하게 이식 필요.
아래 **줄번호는 전부 경량빌드(`mongdal-light.html`) 기준**이며, PC 멀티파일에서는 함수명으로 찾는 게 정확함(파일 분리로 줄번호 안 맞음).

---

## 1. 특수강화 UI 슬롯 분리

**대상 파일:** `js/scenes/game.js` (HUD 렌더링부, 기존 "왼쪽 세로 슬롯 패널" 섹션)
**경량빌드 줄번호:** 특수강화 미니슬롯 신설 `13019~13040`, 일반 스탯 4칸 필터 `13057`

- 주무기 슬롯 3개 각각 아래에 전용 특수강화 미니슬롯 신설 (아이콘 + `레벨/최대치` 표시)
- 기존 4칸짜리 일반 스탯 HUD 표시에서 `isSpecial:true` 항목 제외 → 일반 스탯(atk/hp/mov/atkSpd/cd/vampire/magnet) 4개만 표시
- 이유: 기존엔 `window.statSlots` 하나를 일반 스탯 + 무기별 특수강화가 4칸을 나눠 쓰는 구조라, 특수강화 고르면 일반 스탯 슬롯이 밀려나 보이는 문제였음 → 근본 원인은 표시 로직 문제였고 실제 스탯 적용 자체엔 문제 없었음

---

## 2. 각성(Awakening) 시스템 재설계 — **가장 중요한 변경**

**대상 파일:** `js/systems/weapons.js` (WeaponInstance, computeWeaponGrowth), `js/scenes/blacksmith-scene.js` (표시/비용 함수), `js/scenes/game.js` (런 시작 초기화 `_makeMainWeapon`, 레벨업 카드 라벨)

**경량빌드 줄번호 요약**
| 위치 | 줄번호 |
|---|---|
| `computeWeaponGrowth()` 신설 | `7115~7130` |
| `WeaponInstance.upgrade()` | `7149~7172` |
| 대장간 `upgradeCost~getLvLabel` | `8484~8527` |
| `_makeMainWeapon` (런시작 초기화) | `11719~11729` |
| 인게임 각성 카드 라벨(`isAscend` 분기) | `13152~13180` |

### 기존 문제
- 인게임 레벨업에서 4렙 이후 "각성강화" 카드는 `ascendLv`(쿨타임 배율)만 올리고 실제 각성 효과(`_awakLv`, 오브개수/발사수 등에 쓰이는 값)는 전혀 안 올랐음 → 각성이 사실상 작동 안 함
- 대장간의 각성 공식(`getAwakening(lv) = lv-4`)은 "1강 = 1각성 직행" 구조였는데, 이게 원래 의도(4강마다 1각성)랑 안 맞았음

### 확정된 새 구조 (대장간·인게임 공용)
```
Lv1 → Lv2 → Lv3 → Lv4         (기초, 4픽)
0각성 ~ 4각성 각 4픽씩          (총 16픽, 오브개수/발사수 등 실제 테이블 변화)
5각성                          (1픽만에 도달, 마지막 테이블값 고정)
6각성부터                      (매 픽 데미지 +8%만 상승, 테이블 변화 없음)
```
누적 21픽으로 5각성 도달, 22픽부터 6각성.

### 신설 함수 `computeWeaponGrowth(totalLv)` — `js/systems/weapons.js`
```js
function computeWeaponGrowth(totalLv) {
  totalLv = Math.max(1, totalLv || 1);
  const lv = Math.min(totalLv, 4);
  if (totalLv <= 4) return { lv, ascendLv: 0, awakLv: 0, awakSubLv: 0, overSteps: 0, overAwkDmg: 1 };
  const extra = totalLv - 4;
  const ascendLv = Math.min(extra, 17);
  const awakLv = extra <= 16 ? Math.floor(extra / 4) : 5;
  const awakSubLv = extra <= 16 ? (extra % 4) : 0;
  const overSteps = extra > 17 ? extra - 17 : 0;
  const overAwkDmg = 1 + overSteps * 0.08;
  return { lv, ascendLv, awakLv, awakSubLv, overSteps, overAwkDmg };
}
```
대장간 누적 강화값(`bsLv`)과 인게임 픽 카운트 둘 다 이 함수 하나로 환산. (0~4각성=4픽/단계, 5각성=1픽, 6각성부터 `overAwkDmg`만 상승 — 시뮬레이션 검증 완료, `1~24픽` 전 구간 수동 대조함)

### `WeaponInstance.upgrade()` 재작성
```js
upgrade() {
  if (this.lv < this.maxLevel) { this.lv++; return; }
  const curAwk = this._awakLv || 0;
  if (curAwk < 4) {
    this.ascendLv++;
    this._awakSubLv = (this._awakSubLv || 0) + 1;
    if (this._awakSubLv >= 4) { this._awakSubLv = 0; this._awakLv = curAwk + 1; }
  } else if (curAwk === 4) {
    this.ascendLv++;
    this._awakLv = 5;
  } else {
    this._overAwkDmg = (this._overAwkDmg || 1) + 0.08;
  }
}
```
- 서브웨폰은 `upgrade()`에서 `else` 분기(4렙 이후)에 절대 안 들어감(항상 maxLevel 미만일 때만 카드 노출되므로) → 서브웨폰 로직에 영향 없음 확인함

### 런 시작 초기화 (`_makeMainWeapon`, `js/scenes/game.js`)
대장간에 저장된 `bsLv`를 `computeWeaponGrowth()`로 환산해서 `inst.lv/_awakLv/_awakSubLv/ascendLv/_overAwkDmg`에 그대로 대입하도록 교체. (기존엔 "1강=1각성" 낡은 공식으로 잘못 환산하던 부분)

### 대장간 표시 함수 (`js/scenes/blacksmith-scene.js`)
- `getAwakening`, `getAwakeningDisplay`, `getLvInTier` → 전부 `computeWeaponGrowth()` 위임으로 교체
- `getLvLabel` → 별 개수 + "(n/4)" 진행도 힌트 + 데미지 보너스 표시로 갱신
- `upgradeCost(currentLv)` → `tier = currentLv` 로 단순화 (수치상 기존과 동일, 비용 곡선 변화 없음 — 확인 필요 없음)

### 인게임 각성 카드 라벨 (`pickLevelUp` 표시부, `js/scenes/game.js`, `isAscend` 분기)
- "0각성(2/4)", "5각성 MAX!", "데미지 +16%" 등 실제 진행 상태 반영하도록 재작성
- `curAwk<5`일 때만 "쿨타임 × 0.9" 문구 표시 (6각성 이후엔 데미지 문구만)

### ⚠️ 테스트 필수
- 실제 무한던전에서 4렙→각성 반복 픽업하면서 **5각→6각 넘어가는 시점에 오브개수/발사수는 고정되고 데미지만 오르는지** 육안 확인
- 대장간에서 미리 강화해둔 상태(예: bsLv=10)로 시작 시 인게임 각성 상태가 정확히 이어지는지 (2각성 2/4 상태로 시작해야 함)

---

## 3. 로비 조이스틱 전역 터치 버그 수정

**대상 파일:** `js/scenes/lobby.js` (`bindControls()` 내부)
**경량빌드 줄번호:** `touchstart` 가드 `9939~9941`, `_md`(mousedown) 가드 `9974~9975`, `moveJoyWrapTo` 가드 `9924`

### 증상
대장간 등 메뉴 화면에서 버튼 아닌 영역 터치 시 콘솔 에러:
```
Uncaught TypeError: Cannot read properties of null (reading 'getBoundingClientRect')
```
+ 잠재적으로 해당 화면 스크롤 막힘 (과거 동료/펫 탭에서 겪었던 것과 동일 원인)

### 원인
로비 전용 조이스틱(`#scene-area`, `#joystick-wrap`, `#joystick-knob`)의 `touchstart`/`mousedown` 리스너가 `document`에 전역 등록되고 씬 전환 시 해제가 안 됨. 로비를 벗어나 다른 화면(대장간 등)으로 가면 `#scene-area`가 DOM에서 사라지는데, 리스너는 계속 살아있어서 `moveJoyWrapTo()`가 null 참조로 터짐.

### 수정 (3곳, 전부 `#scene-area` 존재 여부로 가드)
```js
// touchstart 핸들러 최상단
if (!document.getElementById('scene-area')) return;

// _md(mousedown) 핸들러 최상단
if (!document.getElementById('scene-area')) return;

// moveJoyWrapTo() 내부 (방어적 이중 가드)
const sa = document.getElementById('scene-area');
if (!sa) return;
```
- `#scene-area`는 로비 전용 DOM이라, 이 가드는 "로비가 아닌 모든 메뉴 화면(대장간/스테이지선택/던전선택/건물강화/펫/캐릭터 등)"에 자동으로 공통 적용됨. 화면별 예외 클래스 추가 방식(`.companion-list`처럼) 대신 근본 원인 차단으로 처리.
- 전투 화면 자체 조이스틱은 이 코드와 완전히 별개 구현이라 영향 없음.

---

## 4. 점검만 하고 보류 — 아직 미반영

### "쿨타임 × 0.9" 각성 카드 문구가 전 주무기에서 죽은 텍스트
- 주무기 5종(부적/신검/신궁/지팡이/영혼낫) 전부 `levels[].cooldown:0` + 자체 내부 타이머(`_mainCd`/`_splitCd`/`_scytheCd`)로 발사 주기 관리
- `WeaponInstance.tick()`의 `ascendCdMult`(0.9^ascendLv)는 `lvl.cooldown`에만 곱해지는데 그 값이 항상 0이라 **각성해도 실제 쿨타임 감소 없음** — 리밸런싱 과정에서 무기 구조가 "cooldown 직접 사용"에서 "내부 mainCd + 볼리 시스템"으로 바뀌면서 카드 문구만 안 따라간 것으로 추정
- **보류 이유:** 신검/부적/영혼낫까지 마저 점검 끝난 뒤, 무기별 실제 효과(화살수/오브개수/범위 등)를 동적으로 보여주는 문구로 한 번에 교체하기로 함
- 점검 완료: 신궁🏹, 무당 지팡이🪄 (아래 5번 참고)
- 점검 대기: 신검⚔️, 부적📜, 영혼낫🌙

---

## 5. 참고용 상세 데이터 (분석만, 코드 변경 없음)

무당 지팡이 / 신궁 Lv1~7각성 전체 수치표 + 특수강화 별도표를 대화 중 작성함 (오브개수, 화살수, 부채꼴 각도, 데미지 등 단계별 수치). PC 작업 시 참고용으로 필요하면 대화 로그에서 재확인.

---

## 6. 시즌2 유명계 특화 시스템 설계 확정 (코드 미반영, 설계만) — `MOBILE_IDEAS.md`에 상세 반영됨

영혼 수집 시스템(기존 구현 완료) 다음 단계로 아래 항목들 설계 확정. **영혼 제물은 기획에서 제외.**

### 6-1. 전용 동료/펫 4종 + 해금 로드맵

| 시점 | 해금 | 비고 |
|---|---|---|
| 챕터11 클리어 | 해원맥(동료, tank) + 저승나비(펫) | "길잡이" 컨셉 |
| 챕터16 클리어 | 강림차사(동료, assassin) | 보스 "오염된 강림도령" 정화 후 영입 서사 |
| 챕터20 클리어(바리공주) | 상사화(펫, 의인화) | 바리공주 애완 꽃, 최종보상 서사 |

- 해원맥/강림차사: 기존 동료 포맷(`role`+5단계 `awakening`) 그대로, `rarity:'special'`
- 저승나비: `effect:'soulBoost', value:0.20` (영혼 조각/영혼석 획득량 +20%)
- 상사화: `effect:'autoRevive', value:1` (사망 시 1회 자동부활) — ⚠️ **정정:** 코드의 `eff_revive`/`eff_autoRevive` 문자열은 확인해보니 실제로 어디서도 참조 안 하는 죽은 번역 텍스트였음. 재사용 불가, 신규 구현 필요 (아래 6-4 참고)
- 해금 연출: 기존 "첫 동료"/"첫 펫" 팝업 패턴 재사용, 스테이지110/160/200 클리어 트리거
- 스프라이트·공격모션은 원칙대로 "뼈대 완성 후 이미지 작업 단계"에서 일괄 진행 (지금은 스킵) → **[UPDATE] 이미지 작업은 완료됨 (아래 6-4 참고), 코드 통합만 남음**

### 6-2. 명부 강화 (신목 강화 시즌2 확장판, 영혼석 소모)

기존 `CONFIG.SINMOK` 구조 그대로 복제 + 재화만 영혼석으로 교체, 스탯 2종:

```js
SINMOK_S2: {
  BASE_COST: 3, COST_MULT: 1.6,
  MAX_LV:  { extraDmg: 10, reflectDmg: 10 },
  PER_LV:  { extraDmg: 3, reflectDmg: 2 },
}
```
- 추가 데미지: 레벨당 +3% (최대 +30%)
- 데미지 반사: 레벨당 +2% (최대 +20%)
- 시즌2 해금 전 잠금, UI는 신목 화면 서브탭 후보

### 6-3. 무기 초월 (각성과 완전 별개 트랙, 1~10성)

`inst.transcendLv`(0~10, 대장간 전용 영구치)로 `_awakLv`(각성)와 독립 관리. 해금 조건: 해당 무기 5각성 + 시즌2 클리어.

| 단계 | 재료 | 변화 | 그래픽 |
|---|---|---|---|
| 1~3성 | 영혼석+골드 | 수치 강화(화살수/오브수 등 무기별 고유) | 은은한 점멸 광원(이미지 없음) |
| 4~8성 | 영혼석+강화석+골드 | 이펙트 변화 | 더 밝은 광원(이미지 없음) |
| 9~10성 | 영혼석+차원석+강화석+골드 | 그래픽+공격방식 변화, 10성=완전체 | 9성에서 이미지 신규 교체, 10성은 동일 이미지+광원 최대치 |

- 필요 아트: **무기 5종 × 1장**(9~10성 공용)만 있으면 됨
- 무기별 최종형태: 부적=영혼폭풍 / 검=명부의 검 강림 / 활=영혼폭우 / 지팡이=영혼블랙홀 / 낫=사신강림

### 6-4. 실제 구현 체크리스트 (이미지 완성 후 필요한 작업, 코드 위치 확인 완료)

**이미지 자체는 완료됨** (해원맥/강림차사/저승나비/상사화 4종 픽셀아트 확정). 아래는 코드 통합에 필요한 작업.

#### ① 이미지 등록 — `SPRITES.companions` / `SPRITES.pets` (경량빌드 707/955 라인 부근)
해원맥·강림차사는 청아/금강/백호/소희와 같은 `rarity:special` 방식 — 시트 크롭(spriteX/Y) 없이 **개별 이미지 통째로** 사용:
```js
companions: {
  haewonmaek: { src:'...', drawW:70, drawH:75, offsetX:-35, offsetY:-75 },
  gangnim:    { src:'...', drawW:62, drawH:80, offsetX:-31, offsetY:-80 },
},
pets: {
  jeoseung_nabi: { src:'...', drawW:34, drawH:36, offsetX:-17, offsetY:-36 },
  sangsahwa:     { src:'...', drawW:??, drawH:??, offsetX:..., offsetY:... }, // 아래 ⚠️ 참고
}
```
⚠️ **상사화 사이즈 결정 필요**: 원화가 사람 크기 캐릭터인데, 기존 펫은 전부 `drawH 32~52`의 작은 플로팅 아이콘(호야/까마귀 등). 그대로 축소해서 넣을지, 좀 더 큰 미니 동료급으로 보여줄지 정하고 진행.

#### ② 데이터 엔트리
- `companions:` 배열에 해원맥/강림차사 추가 (기존 6-1 각성표 그대로)
- `pets:` 배열에 저승나비/상사화 추가 (`effect`/`value`)

#### ③ 신규 코드 — 펫 효과 2종 (기존 시스템에 없음, 새로 작성 필요)
- **`soulBoost`** (저승나비): `applyPetPassives()`(경량빌드 5527줄)에 케이스 추가 + 영혼 획득 지점인 `_collectSoulDrop()`(11945줄)에 배율 곱셈 로직 삽입
  ```js
  case 'soulBoost': player._soulMult = 1 + pd.value; break;
  ```
- **`autoRevive`** (상사화): `Player.takeDamage()`의 `hp<=0` 분기(3933줄 부근)에 자동부활 체크 신규 삽입
  ```js
  if (this.hp <= 0) {
    if (this._autoReviveCharges > 0) {
      this._autoReviveCharges--;
      this.hp = Math.floor(this.maxHp * 0.5);
      this.iframe = 2.0;
    } else { this.hp = 0; this.dead = true; }
  }
  ```
  `applyPetPassives()`에 `case 'autoRevive': player._autoReviveCharges = (player._autoReviveCharges||0) + pd.value; break;` 추가

#### ④ 신규 코드 — 동료 5성 복합 각성 스킬 (기존 구조상 개별 구현 필수)
공속/범위/HP 같은 단순 수치형은 자동 적용되지만, 복합형 스킬은 플래그만 저장되고(`this['awk_'+type]=val`) 실제 발동 로직은 각 동료마다 따로 짜야 함 (도치의 "처형기", 가온의 "3연속 암살"과 동일 패턴):
- 해원맥 5성 "명부의 철퇴": 강타 적중 시 넉백 + 아군 보호막
- 강림차사 5성 "강림의 심판": 일정 체력 이하 전체 대상 광역 처형
- 두 동료 다 `atkType`도 기존 것(slash/shuriken 등) 재사용할지, 망치·생사부 전용 신규 타입 만들지 결정 필요

#### ⑤ 해금 트리거
스테이지110/160/200 클리어 시점에 `saveData.companions`/`saveData.pets`에 id push + 팝업 — 기존 "첫 동료"/"첫 펫" 팝업 로직 재사용

### ⚠️ 시즌2 설계 To-Do (다음 PC 세션)
- [ ] ① SPRITES에 4종 이미지 등록 (상사화 사이즈 먼저 결정)
- [ ] ② companions/pets 데이터 배열에 4종 엔트리 추가
- [ ] ③ `soulBoost`/`autoRevive` 펫 이펙트 신규 구현 (`applyPetPassives`, `_collectSoulDrop`, `Player.takeDamage`)
- [ ] ④ 해원맥/강림차사 5성 복합 스킬 개별 구현 + atkType 결정
- [ ] ⑤ 스테이지110/160/200 클리어 훅에 해금 트리거 연결
- [ ] `SINMOK_S2` 설정 추가 + 신목 UI에 명부강화 서브탭 추가 (기존 `SINMOK_INFO()`/강화핸들러 복제)
- [ ] `saveData.weaponTranscend` 저장구조 설계 + 대장간에 "초월" 탭 추가
- [ ] glow 이펙트 강도 파라미터화(1~3성 vs 4~8성)
- [ ] 무기 5종 9~10성 이미지는 이미지 작업 단계로 보류

---

## 7. `_cdReduction` 3중 충돌 버그 수정 (Fable5 정밀 스캔에서 발견, 경량빌드 반영 완료)

**대상 파일:** `js/systems/weapons.js`(STAT_UPGRADE_DEFS 부근) + `js/entities/pet-entity.js`(applyPetPassives) + `js/scenes/game.js`(런 시작 초기화)
**경량빌드 줄번호:** 펫 패시브 `5530~5535`, 스탯 정의+recalc 함수 `6854~6871`, 런 시작 리셋 `11724`

### 원본의 문제
쿨감 필드 하나(`player._cdReduction`)에 서로 다른 방식으로 쓰는 코드 3곳:
- 공격속도 스탯: `= min(0.6, lv*0.08)` **절대값 덮어쓰기**
- 쿨타임 스탯: `= min(0.6, prev+0.06)` 증분 (lv 무시)
- 펫 cooldown: `= pd.value` **덮어쓰기** (런 시작 시)

**증상:** ① 여우령(쿨감 18%) 장착 후 인게임에서 공격속도 Lv1 찍으면 쿨감이 18%→8%로 **떨어짐** (펫 효과 증발). ② 같은 카드 조합도 픽 순서에 따라 최종 수치가 최대 2.5배 차이. ③ 하드모드 펫 3슬롯에서 같은 effect 펫들이 중첩 안 되고 마지막 것만 적용 (defense/crit/xp_boost/cooldown 전부, magnet만 정상).

### 수정 내용
```js
// 소스별 전용 필드로 분리 + 통합 재계산
function recalcCdReduction(p) {
  p._cdReduction = Math.min(0.6, (p._cdrAtkSpd||0) + (p._cdrCd||0) + (p._cdrPet||0));
}
// atkSpd: p._cdrAtkSpd = lv*0.08; recalcCdReduction(p);
// cd:     p._cdrCd     = lv*0.06; recalcCdReduction(p);
// 펫:     p._cdrPet   += pd.value; recalcCdReduction(p);
```
- 펫 패시브 defense/crit/xp_boost도 `=` → `+=` 누적으로 변경 (다중 펫 중첩 지원). crit의 `_critMult`는 `Math.max(기존,1.8)`
- 런 시작 초기화에 `_cdrAtkSpd/_cdrCd/_cdrPet = 0` 리셋 추가
- 시뮬레이션 검증 완료: 펫+스탯 조합 0.26, 픽 순서 무관성, 60% 캡, 쿨감펫 2마리 중첩(0.38) 전부 통과

### 남은 논의거리 (설계 판단 필요)
- "공격속도" 스탯이 desc만 공속이고 구현은 쿨감 — 쿨타임 스탯과 사실상 동일 스탯 2개가 노출 중. 진짜 공속(atkInterval 감소)으로 차별화할지, 하나로 합칠지 GDD 기준 결정 필요

---

## 8. Fable5 정밀 스캔 — 기타 발견 (기록만, 미수정)

- **`</html>` 닫는 태그 없음** (파일 끝이 `</body>`): 브라우저는 관대하나 validator/패키징 도구에서 걸릴 수 있음. PC 원본 소스도 같은지 확인
- **setTimeout 32건 vs clearTimeout 10건**: 씬 전환 시 잔존 타이머가 이미 제거된 DOM을 건드릴 가능성 (대부분 짧은 UI 연출이라 위험도 낮음, 필요시 씬별 타이머 레지스트리 고려)
- **`player._cdReduction || window._cdReduction || 0` (L7153 부근)**: falsy 폴백이라 player 값이 0일 때 window 값으로 넘어가는 함정 구조 (현재는 무해)
- **로비 `exit()`가 키보드 리스너만 해제**하고 터치 계열은 잔존 — 3번 항목의 `#scene-area` 가드로 실질 무해화됨 (구조적으로는 exit에서 터치 리스너도 해제하는 게 더 깔끔)
- 구조 무결성 검사 통과 항목: 중복 최상위 선언 없음 / DOM id 충돌 없음 / i18n ko·en 키 172쌍 완전 일치 / 스테이지 id 중복 없음 / 미사용 함수 없음

---

## 9. 스테이지 킬타겟 페이싱 재설계 (설계 논의 중, 미확정 — 2가지 결정 필요)

### 배경 — 발견한 문제
- 유저 체감: "사람들이 1스테이지도 안 하는 것 같다, 지루해서" + "3배속 5분+ 해도 스테이지5를 잘 못 민다"
- 원인 분석 결과 2가지:
  1. **배속(speedMult)은 킬타겟 달성 난이도에 영향 없음** — `dt=rawDt*speedMult`로 `timeLeft`도 배속만큼 같이 줄어들어서, 게임 내부 300초는 배속 무관 항상 300초 고정. 배속은 그냥 실시간을 압축해 보여줄 뿐.
  2. **현재 킬타겟 구조가 챕터마다 300→750로 리셋 반복** (경량빌드 확인: 챕터1~10, 시즌2 챕터11~ 전부 동일 패턴). 시간제한은 항상 300초 고정인데 킬타겟은 스테이지10에서 750까지 올라가 → 초당 요구 킬레이트가 스테이지1(1.0/s)에서 스테이지10(2.5/s)까지 가파르게 상승. 스폰 시뮬레이션 결과, 500킬 목표(스테이지5)는 초반 스폰 램프업(175초 걸려 최소 스폰간격 도달) 때문에 단순평균(1.67/s)이 아니라 실질 약 2.0/s를 꾸준히 유지해야 겨우 달성 가능 — 초반 화력 약한 시점엔 타이트함

### 제안한 새 구조 (논의 중)
챕터별 리셋 없이 **시즌 100스테이지 전체에 연속 램프**, 난이도별 시작값 다르게:
```
killTarget(n) = 시작값 + 5×(n-1)   (n = 시즌 내 스테이지 순번 1~100)

이지: 시작값 30  → 스테이지100 = 525
노말: 시작값 70  → 스테이지100 = 565
하드: 시작값 100 → 스테이지100 = 595
```
- 미들보스/진보스 스테이지는 기존 로직 그대로 유지 (일반킬 채운 뒤 보스 스폰 → 보스 처치까지 해야 클리어, `killTargetReached` 플로우 안 건드림)
- 시즌 전체 총 킬량: 기존 52,500킬(챕터 리셋 ×10) → 신규 약 27,750킬(이지 기준 등차수열 합)로 거의 절반 감소 → 초반 스테이지 체감 속도 대폭 개선

### ⚠️ 미확정 — 다음 세션에서 결정 필요
1. **이지 최종값**: 525(공식 그대로, 5×99=495+30) vs 530 정확히 맞추려면 시작값을 35로 조정(35+5×99=530) — 어느 쪽?
2. **난이도별 격차 유지 여부**: 지금 제안대로면 시작점(30/70/100)은 하드가 이지의 3.3배인데, 스테이지100(525/565/595)에선 1.13배로 격차가 확 좁혀짐. 끝까지 격차 유지하려면 난이도별 증가폭을 다르게(예: 이지+5, 노말+7, 하드+9) 벌려야 함 — 격차 유지 vs 지금처럼 후반에 수렴, 둘 중 선택 필요

### 구현 시 참고
- 현재 200개 스테이지(시즌1 100 + 시즌2 100) 데이터에 `killTarget` 값이 전부 하드코딩 중복돼 있음 → 이번 기회에 `getKillTarget(globalStageIndex, difficulty)` 공식 함수로 통합 리팩터 제안 (숫자 하나로 전체 밸런스 조정 가능해짐). 하드코딩 유지할지 공식화할지도 위 2가지 결정과 함께 정하면 됨
- 초반 스테이지는 스폰 가속 로직(175초 램프업)이 다 돌기도 전에 클리어될 것 — 의도된 부작용(초반=빠르고 가벼움), 버그 아님

---

## 다음 세션 To-Do
- [ ] 위 코드 변경사항(1·2·3·7번) PC 멀티파일 구조에 이식 + 빌드 검증
- [ ] 각성 5→6각 전환 시 데미지만 오르는지 실플레이 검증
- [ ] 신검/부적/영혼낫 특수강화·각성 효과 점검 (신궁/지팡이와 같은 방식)
- [ ] 전 무기 점검 끝나면 "쿨타임 × 0.9" 문구 → 무기별 동적 효과 문구로 일괄 교체
- [ ] 시즌2 특화 시스템(6번 항목) 구현 착수 — 순서 추천: 명부강화 → 동료/펫 해금 → 무기초월
- [ ] **9번 항목 2가지 미확정 사항 결정 후 킬타겟 페이싱 재설계 착수** (이지 525 vs 530 / 난이도 격차 유지 여부)

