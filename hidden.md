# 히든 콘텐츠 기록 (스포일러 주의)

공개 패치노트/설계문서에는 올리지 않는 히든 요소를 여기 모아둔다. 앞으로 늘어날 예정.

## 1. 애기 도깨비 박수 = 꺽쇠의 아이 (2026-07-17)

동료 `baksu`(애기 도깨비 박수)는 사실 `ggeogsoe`(도깨비 수리공 꺽쇠)의 아이다.
도깨비는 나이를 딱히 먹지 않는 설정이라, 부모/자식이어도 겉보기 나이대가 비슷하게 보인다.

- 게임 내 설명 텍스트(`game-data.js` companions.baksu.desc)에는 이 관계를 직접 명시하지 않음 — 플레이어가 유추/발견하는 재미로 남겨둠.
- 시즌3 엔딩(`ending-scene.js` SLIDES_S3, 4슬라이드 중 3번째)에 꺽쇠의 대사로 살짝 흘려둠: "...저 녀석한테도, 언젠가는 말해줘야겠지." — 직접 언급은 아니고 힌트 수준.
- **[UPDATE 2026-07-26] 구현 완료**: 꺽쇠+박수 동시 편성 시 부자 동반 히든 시너지 — 아래 #2 참고.

## 2. 박수+꺽쇠 부자 동반 히든 시너지 (2026-07-26)

**조건**: 동료 `baksu`(박수)와 `ggeogsoe`(꺽쇠)를 **동시에 편성**.

**효과**:
- 둘 다 공격 쿨타임 **절반**
- 대형(포메이션) 각도를 서로 같게 맞춰서 **항상 붙어다님** (반경만 살짝 차등 — 박수가 꺽쇠보다 플레이어에 더 가깝게)
- 시각적 크기: 꺽쇠는 **1.25배로 커지고**, 박수는 **0.8배로 작아짐** — "아빠와 아이" 느낌 강조

**구현 위치**: `mongdal/js/scenes/game.js` `init()` 내 동료 로딩 직후 (도깨비불 시너지 계산 바로 앞) — `atkInterval`/`orbitAngle`/`formationAngle`/`orbitRadius`/`formationRadius`/`_sizeMult` 직접 오버라이드. 크기 배율 적용은 `mongdal/js/entities/companion-entity.js` `draw()`의 `ctx.scale(this._sizeMult, ...)`.

인게임 어디에도 힌트 텍스트 없음 — #1의 대사 힌트를 발견한 플레이어가 실제로 둘을 같이 편성해보면 알아차리는 구조.

## 3. 도깨비불 강화 히든 콤보 (2026-07-17)

**조건**: 아래 동료 중 1명 + 펫 중 1마리를 동시 장착한 상태로, 보조무기 **도깨비불**(`goblin_fire`)을 선택하면
도깨비불의 **지속시간 2배, 시각 크기 2배**로 강화된다. (데미지량 자체는 그대로 — 크기/유지시간만 증가)

- 동료(택1): `ggeogsoe`(꺽쇠) / `baksu`(박수) / `janggu_aebi`(장구애비) — 도깨비 계열 동료 3종 중 아무거나
- 펫(택1): `ssari`(싸리) / `gongi`(공이) — 도깨비 계열 펫 2종 중 아무거나

**구현 위치**:
- 조건 판정: `mongdal/js/scenes/game.js` `init()` 내 `player._dokkaebiFireBoost` 플래그 계산 (동료/펫 로딩 직후, 오행 시너지 계산 직전)
- 실제 적용: `mongdal/js/systems/weapons.js` `WEAPON_DEFS.goblin_fire.fire()` — `life`/`radius` 2배, `Projectile._fireBoosted` 플래그로 시각 스케일(4배→8배)도 같이 반영

인게임 어디에도 힌트 텍스트 없음 — 순수 발견형 히든.

## 4. 히든 시너지 대량 추가 — 서사 기반 16종 (2026-07-26)

프로젝트 정리용 "동료/펫 원 개체·숨긴 속성" 표를 바탕으로 한 번에 브레인스토밍 후 구현한 히든 시너지 묶음.
전부 인게임 힌트 텍스트 없음(순수 발견형). 구현은 `game.js` `init()`의 "히든 시너지 배치1~4" 주석 블록 4곳에 나뉘어 있음.

### 4-1. 단순 스탯형 (배치1)

| 조합 | 효과 |
|---|---|
| 백운선인 + 매화검선 (사문) | 매화검선 공격속도 대폭 상승(쿨타임 ×0.65) |
| 싸리 + 공이 (도깨비 남매 펫) | 싸리 자동수집 범위/속도 +30%, 공이 회복량 +30% |
| 청아 + 성린(펫) (진짜/가짜 용) | 성린 치명타율 +10%p, 색상이 파랗게 물듦(용족 동경) |
| 강다리(펫) + 저승나비(펫) (길잡이 계열) | 플레이어 이동속도 +8% |
| 백호 + 호야(펫) (호랑이 계열) | 백호 궁극기 쿨타임 -20% |

구현: `game.js` companions/petEntities 인스턴스 직접 오버라이드. 성린 크리티컬은 `activePetData`의 `value`를 미리 올려 `applyPetPassives()`가 읽게 함.

### 4-2. 보조무기 훅 (배치2)

| 조합 | 효과 |
|---|---|
| 청아 + 드라고(펫) (용용 조합) | 보조무기 용왕 물줄기(`water_jet`) 회오리 생성 개수 2배 |
| (해원맥 or 강림차사) + (저승나비 or 상사화) | 보조무기 저승낫(`scythe_sub`)이 튕길 때마다 작은 조각 2개 분열(각 50% 피해) |
| 환생동자 + 영혼불씨(펫) (영혼 조합) | 보조무기 귀신손(`ghost_hand`) 크기 1.4배 (실제 판정 radius 포함) |
| 꺽쇠+박수+장구애비(동료 3종 풀장착) + 도깨비+싸리+공이(펫 3종 풀장착) — 하드 난이도 3/3 슬롯 필요 | 도깨비불(`goblin_fire`) 사용 시 화면 전체에 작은 도깨비불 6개 추가 산발(각 50% 피해) — "도깨비 대잔치" |

구현: `player._waterJetDoubleBoost`/`_reaperSplitBoost`/`_ghostHandSizeBoost`/`_dokkaebiFullSetBoost` 플래그를 `weapons.js`의 각 `fire()`에서 읽음. 저승낫 분열은 실제 튕김 판정이 `game.js`의 `chainHit` 처리부에 있어서 그쪽에서 직접 스폰.

### 4-3. 강제발동 체인 + 회전 포메이션 (배치3)

- **가온 × 무사** — 그림자 왕을 쫓는 가온과, 사실 그림자 왕이었던 무사. 무사가 공격할 때마다(쿨타임 무시) 가온이 추가 공격을 날림(단방향). 둘은 플레이어를 기준으로 항상 대칭 위치를 유지하며 시계방향으로 계속 회전("안 잡히게 도망 다니는" 연출).
- **아람 × 단비** — 자매. 단비가 힐할 때 아람이 강제 공격(쿨 무시), 아람이 공격할 때 단비가 강제 힐(쿨 무시) — 서로 챙겨주는 티키타카. 포메이션은 "천천히 붙었다가 훅 멀어졌다가 다시 천천히 붙는" 3.3초 주기 반경 진동(자매끼리 티격태격하는 느낌), 항상 마주보는 각도.
- **매화검선 × 허무검사** — 형제. 한쪽이 공격하면 반대쪽의 궁극기가 쿨타임 무시하고 강제 발동(양방향) — "형제의 합공". 기본 포메이션은 항상 서로 대칭.
- **(해원맥 or 강림차사) × (상사화 or 저승나비)** — 보스가 화면에 있는 동안(`window._boss` 존재) 해당 동료의 궁극기 쿨타임이 기존의 20%로 극단적으로 짧아져 궁극기를 남발함.
- **생령 × 봉황** — 기억을 잃은 주작(생령)과 그를 찾아다니는 봉황. 봉황이 생령 주위를 반경 50px로 계속 맴돌고(플레이어가 아니라 생령이 궤도 중심), 생령이 공격할 때마다 봉황 머리 위에 물음표가 떠올랐다가 2초간 페이드 아웃 — 바로 옆에 있는데도 못 알아보는 아이러니.

구현: `CompanionEntity`에 `_chainLinks`(강제발동 체인 설정)/`forceAtk()`/`forceUlt()`/`forceHeal()`/`_bossRushBoost`/`_questionMarks`를 추가. `game.js` 메인 루프에서 회전 각도는 `update()` 호출 **전**에, 생령 궤도는 `update()` 호출 **후**에 갱신(포메이션 계산 시점 때문). 강제발동 체인은 `update()` 호출 후 2차 패스로 이번 프레임에 실제 발동한 이벤트만 감지해서 처리.

### 4-4. 카메오 난입 — 사신진 / 저승 총출동 (배치4)

동료는 최대 3슬롯이라 4인조 세트를 다 채울 수 없다는 점을 이용한 연출.

- **사신진**: 청아(청룡)+백호(백호신 아들)+수호(실은 현무)+봉황(준-주작) 중 **3명**을 편성하면, 남은 1명이 **20초마다 난입해 10초간 함께 싸우고 사라짐**을 반복. 난입 순간 4명(장착 3 + 카메오) 전원 공격 쿨타임 초기화. 3명만 모여 있어도 상시로 전체 공격력 +5%, 화면 테두리에 옅게 맥동하는 사신 문양 이펙트.
- **저승 총출동**: 해원맥+강림차사(저승사자)+허무검사+환생동자(귀신) 중 **3명** 편성 시 동일한 20초/10초 사이클로 남은 1명이 난입, 등장 시 4명 전원 공격 쿨 초기화.
- 카메오로 난입하는 동료는 실제 보유 성장치와 무관하게 **0성 기준 고정 스탯**으로 등장(전략 요소가 아니라 연출용).

구현: `game.js`에 `_cameoState` 배열(그룹별 `{ids, missingId, phase, t, entity}`)을 두고 매 프레임 타이머 진행. 등장 시 `CompanionEntity`를 `forceBaseStats=true`로 생성해 실제 `companions` 배열에 임시로 push(기존 렌더/충돌/업데이트 파이프라인을 그대로 재사용), 10초 후 splice로 제거. `CompanionEntity` 생성자에 `forceBaseStats` 3번째 파라미터 추가(true면 세이브의 별/각성 무시하고 0으로 고정).

## 5. 스테이지별 해금 요소 정리 (2026-07-18)

스포일러라기보단 참고용 레퍼런스. 스테이지 선택 화면 "무지개 테두리"(`stage-select.js` `UNLOCK_PENDING_STAGE_IDS`), 자동진행 로비강제(`game.js` `LOBBY_CHANGE_STAGES`), 실제 해금 로직(`unlock.js` `UNLOCK_TABLE` + `game.js` 개별 `stageId===` 체크) 세 군데에 흩어져 있어서 한곳에 정리.

### 건물/시설 해금 (`unlock.js` UNLOCK_TABLE, 난이도 무관 최초 클리어 시)

| 스테이지 | 해금 | 비고 |
|---|---|---|
| 5 | 🔨 대장간 | 무기 구매·주무기 변경, 강화석 던전 |
| 10 | 👥 의원당 | 동료 모집, 상점 |
| 15 | ⛩️ 서낭당 | 무한 던전, 보스 러시 |
| 20 | 🗿 장승당 | 건물 업그레이드, 천운석 던전 |
| 25 | 🐉 용왕 연못 | 펫, 천령과 던전 |
| 30 | 🌳 신목 | 영구 강화, 태극석 던전 |

### 동료/펫/슬롯 해금 (`game.js` 개별 `stageId===` 체크)

| 스테이지 | 조건 | 해금 |
|---|---|---|
| 50 | 이지 난이도 클리어 | 이지 2슬롯 (`easy2`) |
| 100 | 이지 난이도, 시즌1 전체클리어 | 이지 3슬롯 (`easy3`) — `season1_end`(unlock.js)와 별개 로직 |
| 100 | 최초 클리어(난이도 무관) | `season1Clear` 플래그 → 차원석 재화·상인 NPC 해금(`unlock.js season1_end`) |
| 110 | 최초 클리어 | 동료 [해원맥](다이아 뽑기풀), 펫 [저승나비](천령과 구매) — "길잡이" 컨셉, `WORLDBUILDING.md` 로드맵 |
| 160 | 최초 클리어 | 동료 [강림차사] |
| 200 | 최초 클리어 | 펫 [상사화], `season2Clear` 플래그 → 혼돈석 던전(`unlock.js hondonseok_dungeon`) |
| 300 | 최초 클리어 | `season3Clear` 플래그 → 순리석 던전(`unlock.js sullriseok_dungeon`) |
| 400 | 최초 클리어 | `season4Clear` 플래그 (현재 후속 해금 없음 — 시즌5용 자리만 있음) |
| 401 | 진입 시(시즌5 콘텐츠 없어 현재 도달 불가) | 삼신할매 태몽 회상 — 시즌5 대비 미리 배선해둔 것, 아직 동작 안 함 |
| 1~20 | 각 스테이지 최초 클리어(난이도 무관) | 초보자 선물(재화 보너스, `beginnerGiftFor()`) |

### 무지개 테두리 표시 스테이지 (`stage-select.js` UNLOCK_PENDING_STAGE_IDS)

`[5, 10, 15, 20, 25, 30, 50, 100, 110, 160, 200]` — 위 표의 해금 트리거 스테이지 전부. **110**(시즌2 첫 보스 "황천 뱃사공")은 해원맥+저승나비 "길잡이" 해금 지점.

**2026-07-18 수정 이력**:
- 50이 이 목록에서 누락돼 있던 걸 발견해서 추가함(이지 2슬롯 해금인데 테두리가 안 뜨고 있었음).
- 110의 실제 해금 로직을 찾아 연결함 — `WORLDBUILDING.md`에 "챕터11 클리어(시즌2 시작): 해원맥+저승나비('길잡이')"로 명시돼 있었는데, 실제 코드는 `saveData.season1Clear`(스테이지100) 기준으로 되어 있어서 설계보다 1챕터 일찍, 스토리 연결고리 없이 풀리고 있었음.
  - `shop-scene.js` `diamondPool()`: 해원맥(다이아 뽑기풀) 게이트를 `season1Clear` → `Unlock.cleared(saveData, 110)`로 변경.
  - `pet.js` `unlockPet()` / 목록 렌더: 저승나비(`season2:true` 펫) 게이트를 동일하게 `Unlock.cleared(saveData, 110)`로 변경.

### 자동진행 시 로비 강제 스테이지 (`game.js` LOBBY_CHANGE_STAGES)

`[1, 5, 10, 15, 20, 25, 30, 100, 110, 160, 200, 300, 400]` — 위 해금들을 로비에서 확인시키기 위한 목적. 여기 없는 일반 보스 스테이지(40, 50, 60...)는 자동진행 시 바로 다음 스테이지로 진행됨. 단, `saveData._showSlotUnlock`/`_showFirstClearDialogue` 플래그가 서 있으면(노말/하드 최초 클리어처럼 스테이지 번호가 고정 안 된 해금) 이 목록에 없어도 동적으로 로비 강제됨.
