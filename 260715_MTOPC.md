# 260715 모바일 → PC 핸드오프

세션: 모바일(Claude.ai)
주제: 원거리 몹 공격 미표시 버그 (원인 확정, 코드 미반영)

---

## 1. 원거리 몹 투사체가 생성되자마자 소실되는 버그

### 증상
원거리 공격 패턴(`attackPattern:'ranged'`)을 가진 몬스터가 사격 모션(거리 유지, 쿨타임)까지는
정상 작동하지만, 실제 투사체가 화면에 전혀 보이지 않고 플레이어에게 원거리 피해도 들어오지 않음.

### 원인 (코드 확인 완료)
`enemy.js`의 `update()`는 원거리 패턴일 때 `_fireProjectile()`을 호출해 투사체 객체를 **리턴**함:

```js
case 'ranged':
  ...
  this.shootCd -= dt;
  if (this.shootCd <= 0 && dist < 350) {
    this.shootCd = 1.8 + Math.random();
    return this._fireProjectile(dx, dy, dist);   // 투사체 객체 리턴
  }
  break;
```

```js
_fireProjectile(dx, dy, dist) {
  const spd = 190;
  return {
    isEnemyProjectile: true,
    x: this.x, y: this.y,
    vx: (dx/dist)*spd, vy: (dy/dist)*spd,
    damage: Math.floor(this.damage * 0.6),
    radius: 5, life: 2.2, dead: false, t: 0,
    color: this.color,
  };
}
```

그런데 `game.js`의 메인 루프에서 `e.update()`의 **리턴값을 아예 받지 않음**:

```js
for(const e of enemies){
  ...
  e.update(dt,player.x,player.y);   // 리턴값 버려짐
  if(!e.dead&&e.hitTest(player.x,player.y,14)){
    player.takeDamage(e.damage, e);
    ...
  }
}
```

`isEnemyProjectile:true` 플래그가 붙어있는 걸 보면 애초에 별도 배열(`enemyProjectiles` 등)에서
처리하려던 설계 흔적으로 보이나, 그 배열/처리 로직 자체가 코드 어디에도 없음 — **구현이 중간에
누락된 상태**로 판정.

### 영향
- 원거리 몹은 사실상 "플레이어가 근접 거리까지 다가가야만"(`hitTest` 근접 충돌) 데미지를 줄 수 있음
- 의도된 "거리를 유지하며 사격하는" 위협 자체가 완전히 무력화됨
- 시즌1부터 지금까지 원거리 패턴 몬스터가 있었다면 전부 영향받았을 것으로 추정 (챕터별 몬스터 정의에서
  `attackPattern:'ranged'`로 등록된 몬스터 전수 확인 필요)

### 확정 수정 방향
1. `enemies` 배열 순회 시 `e.update()`의 리턴값을 캐치해서 별도 `enemyProjectiles` 배열에 push
2. `enemyProjectiles`를 매 프레임 업데이트(이동)하고, 플레이어와 충돌 판정 추가
3. 렌더링 루프에 `enemyProjectiles` 그리기 추가 (플레이어 투사체와 구분되는 색상/스타일 — 이미 `color: this.color`로 몹 고유색은 지정되어 있음)
4. 화면 밖으로 나가거나 `life` 소진 시 제거

### To-Do (코드 구현 시)
- [ ] `game.js` 메인 루프에서 `e.update()` 리턴값 캐치 → `enemyProjectiles.push(...)`
- [ ] `enemyProjectiles` 배열 신규 선언 및 매 프레임 이동/수명 처리 로직 추가
- [ ] `enemyProjectiles` vs 플레이어 충돌 판정 추가 (`player.takeDamage`)
- [ ] `enemyProjectiles` 렌더링 로직 추가 (canvas draw, 플레이어 투사체와 시각적으로 구분)
- [ ] 챕터별 몬스터 정의(`monsters.js`)에서 `attackPattern:'ranged'`로 등록된 몬스터 전수 조사 —
  지금까지 원거리로 설계했던 몬스터들이 전부 이 버그의 영향을 받았을 가능성 높음
- [ ] 수정 후 실플레이로 원거리 몹의 사격이 정상적으로 보이고 피해가 들어오는지 확인

---

### 배경
스테이지1~20 최초 클리어 시 스테이지별 골드/재화 추가 보상(초반 유저 배려용, PC에 상세 설정
있음)을 신규로 셋업했는데, 이미 해당 스테이지들을 예전에 클리어한 기존 유저는 이 보상을 받을 방법이
없음 → 소급 보상용 프로모션 코드 시스템 신설.

### UI 설계
- 로비 설정(⚙️) 메뉴 안에 **"코드 입력"** 버튼 신규 추가
- 클릭 시 텍스트 입력창 + 확인 버튼 팝업 노출
- 성공/실패 결과를 토스트 또는 팝업 메시지로 표시 (실패 사유: 잘못된 코드 / 이미 사용한 코드)

### 코드 정의 — 하드코딩 config
```js
// js/data/promo-codes.js (신규 파일 제안)
const PROMO_CODES = {
  'EARLYBIRD1TO20': {
    gold: 0,   // ⚠️ PC에서 스테이지1~20 보상 합계 확인 후 채울 것
    gems: 0,   // ⚠️ 재화(다이아 등) 합계도 동일하게 확인 필요
    // 필요 시 강화석/영혼석 등 다른 재화도 이 객체에 필드 추가 가능
    desc: '초반 스테이지 보상 시스템 추가 이전 클리어자 소급 보상',
    oneTimeGlobal: true, // 계정당 1회만 사용 가능 (스테이지별 개별 지급 아님, 통합 1회 지급)
  },
};
```

### 중복 사용 방지 로직
```js
saveData.redeemedCodes = saveData.redeemedCodes || [];

function redeemCode(inputCode) {
  const c = inputCode.trim().toUpperCase();
  if (saveData.redeemedCodes.includes(c)) {
    return { ok:false, msg:'이미 사용한 코드입니다' };
  }
  const reward = PROMO_CODES[c];
  if (!reward) {
    return { ok:false, msg:'존재하지 않는 코드입니다' };
  }
  saveData.gold += reward.gold || 0;
  saveData.gems += reward.gems || 0;
  saveData.redeemedCodes.push(c);
  Save.save(saveData);
  return { ok:true, reward };
}
```

### 설계 방향 요약
- **개별 스테이지 단위 소급이 아니라, 통합 1회 코드로 합계 지급** — "1~20스테이지 전부 클리어했다고 가정한 총합"을 코드 하나로 일괄 지급하는 방식이 스테이지별 추적보다 구현이 단순하고 유저 경험도 명확함
- 코드명은 예시(`EARLYBIRD1TO20`)이며 최종 확정 필요
- 향후 다른 프로모션(이벤트/SNS 홍보 코드 등)에도 동일 구조(`PROMO_CODES` 객체에 항목 추가)로 확장 가능 — 이번 소급보상이 시스템 자체의 첫 사용 사례가 되는 셈

### To-Do (코드 구현 시)
- [ ] PC에서 스테이지1~20 보상 합계(골드/재화별) 확인
- [ ] `PROMO_CODES.EARLYBIRD1TO20`(또는 확정된 코드명)에 실제 보상값 채우기
- [ ] `js/data/promo-codes.js` 신규 파일 생성 (또는 기존 데이터 파일에 병합)
- [ ] `saveData.redeemedCodes` 필드 추가
- [ ] 로비 설정 메뉴에 "코드 입력" 버튼 + 팝업 UI 구현
- [ ] `redeemCode()` 로직 연결, 성공/실패 토스트 메시지 구현
- [ ] 코드 배포 방식 결정 (itch.io 페이지 공지? 커뮤니티 게시글?)

---

## 3. 보스러시(+무한던전/재화던전) 자동재도전 시 보상모드 리셋 버그 (원인 확정, 코드 미반영)

### 증상
보스 무한러쉬(및 무한던전/재화던전 등 `gameMode!=='normal'` 전체)를 **자동모드**로 플레이 중
실패하면 자동재도전이 걸리는데, 이 재도전부터 다이아 보상이 골드로 바뀜.

### 원인 (코드 확인 완료)
결과화면 자동진행 로직에서 재도전 호출 시 `rewardMode` 파라미터가 누락됨:

```js
if (gameMode !== 'normal') {
  // 던전/무한/보스러시: 패배 시 같은 모드로 재시작, 승리 시 로비
  if (!victory) SceneManager.go('game', { mode: gameMode, difficulty }); // rewardMode 없음!
  else SceneManager.go('lobby');
}
```

`init()`에서 `_rewardMode = params?.rewardMode || null;`로 초기화하는데, 재도전 호출에 `rewardMode`가
안 실려서 **`_rewardMode`가 매번 `null`(일반모드)로 리셋**됨.

- **최초 진입**(로비/던전선택에서 `rewardMode:'bossrush'` 등을 넘겨줌): 빅골드 드랍이 💎로 정상 전환
- **실패 후 자동재도전**: `_rewardMode=null`로 리셋 → 빅골드 드랍이 골드 그대로 지급 (버그)

수동모드(자동재도전 없음)로는 이 버그를 안 만나고, **자동모드 재도전 시에만** 재현되는 이유가 정확히
이 지점 — 최초 진입 경로와 재도전 경로가 서로 다른 파라미터 셋을 쓰기 때문.

### 영향 범위
`gameMode !== 'normal'` 전체(보스러시/무한던전/강화석·천운석·천령과·태극석 던전)가 전부 영향받을
가능성 있음 — 전부 `rewardMode`로 보상 종류를 구분하는 구조라면 동일 버그 존재.

### 확정 수정 방향
```js
if (!victory) SceneManager.go('game', { mode: gameMode, difficulty, rewardMode: _rewardMode });
```

### To-Do (코드 구현 시)
- [ ] 위 재도전 호출부에 `rewardMode: _rewardMode` 추가
- [ ] 다른 `gameMode!=='normal'` 던전들(재화던전 등)도 동일한 자동재도전 경로를 타는지 확인,
  전부 같은 수정 적용
- [ ] 수정 후 보스러시 자동모드로 일부러 패배시켜서 재도전 시에도 💎 보상이 정상 유지되는지 확인

### 영향 범위 확정 — 6개 던전 전부 해당

`dungeon-scene.js`의 `DUNGEON_DEFS` 확인 결과, 아래 6개 던전 **전부** `mode:'infinite'` 또는
`'boss_rush'`로 등록되어 있어 `gameMode!=='normal'` 조건에 전부 해당 — 자동재도전 시 전부 동일하게
`rewardMode` 리셋 버그의 영향을 받음:

| 던전 | rewardMode |
|---|---|
| 강화석 던전 | `ganghwaseok` |
| 무한 던전 | `infinite` |
| 보스러시 | `bossrush` |
| 천운석 던전 | `cheonunseok` |
| 천령과 던전 | `cheonryeonggwa` |
| 태극석 던전 | `taegeukseok` |

즉 강화석 던전에서 자동모드로 파밍하다 죽으면, 재도전부터는 강화석 대신 그냥 골드가 나오는 등
전 던전 공통 문제. 3번 수정 하나로 6개 던전 전부 해결됨.

---

## 4. 시즌별 특화재화 전용 던전 신설 제안 (설계 검토, 코드 미반영)

### 배경
기존 강화석/천운석/천령과/태극석은 전용 무한던전으로 집중 파밍 가능한데, 오늘 설계한 시즌3 혼돈석은
스테이지 드랍(12%)+보스처치+챕터보너스로만 설계되어 있어 "집중 파밍" 수단이 없음. 기존 재화 던전
패턴과의 일관성을 위해 시즌별 특화재화도 전용 던전으로 확장하는 방향 제안.

### 제안 구조
기존 `DUNGEON_DEFS` 패턴 그대로 재사용:

```js
{
  id: 'hondonseok',
  unlockId: 'dungeon_hondonseok',   // 시즌3 클리어(챕터30) 후 해금
  icon: '🌀', spriteKey: 'hondonseok',
  reward: 'hondonseok', rewardIcon: '🌀', rewardSpriteKey: 'hondonseok',
  mode: 'infinite', rewardMode: 'hondonseok',
}
```

- 시즌3 클리어 후 **혼돈석 던전** 해금
- 시즌4 클리어 후 **순리석 던전** 해금 (동일 패턴)
- 이후 시즌(선계 등)에서도 전용 재화가 생기면 동일 패턴으로 확장

### To-Do (다음 세션 확정 필요)
- [ ] 혼돈석/순리석 던전 해금 조건(챕터30/40 클리어) 최종 확정
- [ ] 던전 내 혼돈석/순리석 드랍량(기존 재화던전 수급 밸런스 참고해서 산정)
- [ ] `unlock.js`에 `dungeon_hondonseok`/`dungeon_sullriseok`(가칭) 해금 항목 추가
- [ ] 3번(rewardMode 버그) 수정과 함께 진행하는 게 자연스러움 — 신규 던전도 같은 코드 경로를 타므로

---

## 5. 신목의 균열 — 해금 조건 확정 + 발견성 UI 예외처리 (설계 확정, 코드 미반영)

### 배경
기존 260707 설계(`MOBILE_IDEAS.md`)에 있던 로어 드립 기능. 로비 신목 근처 검은 균열 오브젝트를
클릭하면 "기록 구슬"(12~15개, 짧은 시적 텍스트+이미지)이 떠 있는 별도 공간이 열려 지금까지의
이야기 흐름을 정리해서 보여주는 선택적 콘텐츠. 해금 조건이 "예시, 미확정"으로 남아있었음.

### 확정 내용
- **해금 조건 = 스테이지123 클리어**로 확정 (기존 "121 예시" 대체)
- **진입 방식**: 스테이지123 클리어 직후, 로비에 신목 옆 `???` 오브젝트가 뜬금없이 생겨남 (사전 예고 없음)
- **클릭 시**: 다른 맵/지도 형태로 별도 공간 진입, 지금까지 진행된 이야기의 흐름을 정리해서 보여줌

### ⚠️ 중요 — 무지개 테두리 발견성 UI(260714 문서 4번)와 상충, 예외처리 필요
260714 문서 4번(해금 트리거 스테이지에 무지개 순환 테두리 표시)은 "여기서 뭔가 풀린다"를 **명시적으로
광고**하는 장치인데, 신목의 균열은 **히든 컨셉**이라 정반대 원칙임. 스테이지123 선택 박스에는 무지개
테두리를 표시하지 않음 — 두 시스템이 서로 다른 발견성 철학(명시적 예고 vs 우연한 발견)을 갖고
공존해야 함.

### To-Do (코드 구현 시)
- [ ] 무지개 테두리(260714 #4) 대상 스테이지 목록에서 스테이지123 명시적 제외 처리
- [ ] 스테이지123 클리어 시 로비에 신목 옆 `???` 오브젝트 생성 로직
- [ ] 균열 클릭 시 진입 연출(화면 흔들림 + 애기씨 팝업) 구현
- [ ] 기록 구슬 12~15개 UI 공간 구현 (기존 MOBILE_IDEAS 설계 예시 텍스트 참고)
- [ ] 기록 텍스트 12~15개 최종 확정 (다음 세션, WORLDBUILDING_v0.5.md 설정 기준)
- [ ] 도트 이미지 12~15장은 이미지 작업 단계에서 진행

---

## 6. 삼신할매의 태몽 (설정 확정, 코드 미반영)

### 배경
세계관 문서 미결설정이었던 "태몽 설정 정식 반영"을 확정. 상세 내용은 `WORLDBUILDING_v0.5.md`
3번(삼신할머니) 섹션에 반영 완료.

### 요약
- 애기씨가 태어나던 날 밤 삼신할매가 목격한 태몽으로 설정
- 문장: **"하나였던 달이 둘로 갈라지더니, 다시 하나로 겹쳐지는 꿈이었다."**
- 애기씨의 본질(물질+반물질 동시 보유)과 최종 서사(자아 합일)를 은유적으로 암시, 스포일러 없음
- 노출 2단계: ①인트로 슬라이드5(삼신할매 등장)에 한 줄 추가 ②시즌5 진입 시 대사팝업으로 전문 회상
- 신규 시스템 불필요 — 기존 인트로 슬라이드 구조 + 대사팝업 구조 재사용

### To-Do (코드 구현 시)
- [ ] `lang.js`의 `intro` 슬라이드5(삼신할매 등장 슬라이드)에 태몽 한 줄 텍스트 추가
- [ ] 시즌5 첫 진입(스테이지401 또는 챕터41 진입) 시 삼신할매 대사팝업 트리거 신규 구현
- [ ] 대사팝업 문구 최종 확정 (제안: 태몽 전문 회상 + "그 꿈이... 무엇을 뜻하는지 이제야 알겠구나")

---

## 7. 순리석 획득 경로 + 시즌4 몬스터 HP/공격력 곡선 확정 (설계 확정, 코드 미반영)

### 순리석 획득 경로 (혼돈석 패턴 재사용)
- **① 몬스터 처치**: 골드 드랍 슬롯 12% 확률로 순리석 대체 (스테이지301~400 조건부)
- **② 보스 확정드랍**: 미들보스 3~5개 / 챕터보스 8~12개
- **③ 챕터 클리어 보너스**: 챕터보스 최초 클리어 시 20~30개
- **④ 소멸스택 부산물**: 환생조각 3개 → 순리석 5개 전환 (6번 섹션 참고)

### 시즌4 몬스터 HP/공격력 곡선 (챕터30 마감치 HP1330에서 ×1.13 복리 이어감)

| 챕터 | HP | 공격력 | XP/골드 |
|---|---|---|---|
| 31 | 1500 | 220 | 143 |
| 32 | 1700 | 240 | 162 |
| 33 | 1920 | 262 | 183 |
| 34 | 2170 | 286 | 207 |
| 35 | 2450 | 312 | 233 |
| 36 | 2770 | 341 | 264 |
| 37 | 3130 | 372 | 298 |
| 38 | 3540 | 406 | 337 |
| 39 | 4000 | 443 | 381 |
| 40 | 4520 | 484 | 430 |

XP/골드는 시즌3와 동일하게 hp÷10.5 비율 유지.

### To-Do (코드 구현 시)
- [ ] `sullriseok`(가칭) 재화 필드 `saveData`에 추가
- [ ] 몬스터 드랍 로직에 시즌4 조건부 순리석 드랍 추가 (①), 보스 확정드랍(②), 챕터클리어 보너스(③)
- [ ] 환생조각→순리석 전환 로직에 5개 지급 반영 (④, 6번과 연동)
- [ ] `monsters.js`의 `byChapter`/`defs`에 챕터31~40 몬스터 HP/공격력/XP/골드 값 반영 (위 표 기준, ±10~15% 배분은 8번 몬스터 설계 참고)

---

## 8. 실체화 몬스터 스탯/보상 확정 (설계 확정, 코드 미반영)

260714 #8(잔상 5초 합체 실체화)의 마지막 미결 항목 확정. 이걸로 시즌4 몬스터 설계 완전 종결.

### ① 실체화 대상 — 현재 챕터 정의 몬스터 중 랜덤 1종 (전용 타입 신설 X)

- 잔상은 시즌1~3 몬스터지만, 실체화되면 **현재 챕터(시즌4) 몬스터로 태어남** —
  "과거의 잔상이 귀허계의 법칙대로 거듭난다"는 테마와 일치
- 구현: `monsters.js`의 `byChapter[현재챕터]`에서 랜덤 1종 뽑아 `new Enemy()` — 이미지/AI/스탯 전부 재사용, 신규 작업 없음
- 부수 효과: `hollow_burst` 타입이 뽑히면 "잔상→실체화→자폭→소멸스택+1" 순환이 자연 완성

### ② 스탯 — 해당 챕터 기준 × 1.8 (HP/공격력 동일 배율) — 260715 수정

**변경 이력**: 최초 설계 ×1.3 → 리스크&리워드 검토 후 **×1.8로 상향**.
×1.3은 "순리석 확정 획득"에 비해 리스크가 너무 약해서 사실상 무조건 이득인
구조가 됨 (도망 선택지가 있는 이상 손해 볼 이유가 없어 긴장감 실종). ×1.8로
올려 "무리하면 실제로 위험한" 수준까지 끌어올려서 진짜 선택의 무게를 부여.
- 시각 구분: 실체화 몹은 옅은 발광 아웃라인 유지

### ③ 보상 — 순리석 확정 1개 + XP/골드 ×1.8 (배율 연동)

- 일반 시즌4 몹의 순리석 12% 확률 드랍과 달리 **100% 확정 1개**
- XP/골드는 상향된 스탯 배율(1.8)과 연동
- 설계 의도: "합체 방치=안전하지만 기회비용, 처치=위험을 감수한 순리석 확정"의
  진짜 리스크&리워드 — 유저가 잔상 근처에서 5초 버티며 합체를 **일부러 유도**하되,
  ×1.8 스탯을 감당할 수 있는 시점(빌드/타이밍)인지 판단해야 하는 구조로 개선.
  소멸스택의 hollow_burst 사냥 유인과 동일한 설계 문법으로 시즌4 정체성 일관 유지

### To-Do (코드 구현 시)
- [ ] 합체 완료 시 `byChapter[현재챕터]` 랜덤 1종으로 `Enemy` 생성 (260714 #8 To-Do의 "구체 타입/스탯" 항목 해소)
- [ ] 생성 시 HP/공격력/XP/골드에 ×1.8 배율 적용
- [ ] 실체화 몹 발광 아웃라인 렌더링 (일반 몹과 시각 구분)
- [ ] 처치 시 순리석 확정 1개 드랍 로직 (기존 12% 확률 로직과 별개 경로)

---

## 10. 버그: 파밍(스테이지 클리어 후 5초) 중 레벨업 시 상태 복귀 오류 (설계 확정, 코드 미반영)

### 증상
스테이지 클리어 → 5초 파밍 화면 중 XP 오브를 먹어 레벨업하면, 이후 파밍이 정상 종료되지 않고 게임이 꼬임 (최악의 경우 클리어했는데도 시간초과로 패배 처리될 수 있음).

### 원인 (`js/scenes/game.js`, `update()`)
```js
function update(dt){
  if(state==='farming'){
    farmingTimer -= dt;
    if(farmingTimer<=0){ endGame(true); return; }
    ...
    for(const o of xpOrbs){ o.update(dt,player.x,player.y,player.magnetRange);
      if(o.dead&&player.gainXp) player.gainXp(Math.floor(o.val||o.value||1));
    }
    ...
    return;
  }
  if(state!=='playing') return;
  ...
}
```
1. 파밍 중(`state='farming'`) XP 오브 흡수 → `player.gainXp()` 내부에서 레벨업 조건 충족 시 `triggerLevelUp()` 호출 → `state='levelup'`으로 강제 전환
2. 게임 루프가 `state==='playing'||'farming'`일 때만 `update()`를 호출하므로, `state='levelup'`이 되는 순간 `farmingTimer` 카운트다운이 멈춤 (파밍 화면 위에 레벨업 카드만 뜬 상태로 정지)
3. 카드 선택 시 `pickLevelUp()`이 **무조건 `state='playing'`으로 복귀** — `'farming'`으로 안 돌아감
4. 결과: `update()`가 "일반 모드(playing)" 분기를 다시 타게 되는데, 이미 `killTargetReached=true`이고 몹도 다 죽인 상태라 아무 일도 안 일어나면서 `timeLeft`만 계속 줄어듦 → 0이 되면 클리어했음에도 시간초과 패배 처리될 위험

### 수정 방향 (확정)
`triggerLevelUp()` 호출 시점에 이전 state가 `'farming'`이었는지 플래그로 기록해두고, `pickLevelUp()` 완료 시 그 값에 따라 복귀 상태를 분기.

```js
function triggerLevelUp(){
  window._wasFarming = (state === 'farming'); // 추가
  state='levelup';
  ...
}

function pickLevelUp(cat, idx){
  ...
  state = window._wasFarming ? 'farming' : 'playing'; // 기존 무조건 'playing' → 분기 처리
  window._wasFarming = false;
  return;
  // (함수 하단의 카드 없을 때 분기도 동일하게 수정)
}
```

### To-Do (PC 세션에서 구현)
- [ ] `game.js`의 `triggerLevelUp()`에 `window._wasFarming` 플래그 기록 추가
- [ ] `pickLevelUp()`의 두 군데 `state='playing'` 하드코딩을 위 분기 로직으로 교체 (카드 선택 시 / 선택지 없을 때 둘 다)
- [ ] `farming` 복귀 시 `farmingTimer`가 멈췄던 지점부터 정상 이어서 감소하는지 확인 (변수 자체는 그대로 유지되므로 별도 리셋 불필요할 것으로 예상 — 실기기 검증 필요)
- [ ] 실플레이 검증: 파밍 중 레벨업 → 카드 선택 → 파밍 화면 복귀 → 5초 후 정상 로비 이동까지 전체 플로우 확인

---

## 11. 레벨업 카드(주무기/보조무기/스탯) 텍스트-실제효과 괴리 점검 (PC 확인 필요, 미착수)

### 배경
무기 성장 시스템(각성/초월 등)을 최근에 크게 갈아엎었기 때문에, 레벨업 선택 카드에 표시되는
**설명 텍스트**와 **실제로 적용되는 효과**가 어긋나 있을 가능성이 높음. 260706 세션에서 이미
"쿨타임 × 0.9" 문구가 실제로는 적용 안 되는 죽은 텍스트였던 사례가 한 번 발견된 바 있음
(당시 `isAscend` 분기 재작성으로 수정됨) — 이런 류의 잔존 괴리가 다른 카드에도 더 있을 가능성.

### 점검 필요 범위
- **주무기 카드**: 강화(Lv업)/각성/초월 단계별 설명 문구 vs `computeWeaponGrowth()` 기준 실제 스탯 변화
- **보조무기 카드**: 신규 획득/강화 시 설명 문구 vs 실제 효과치
- **스탯 카드**: `atk/spd/def/mov/eva/cd` 등 각 스탯 설명(`WEAPON_I18N` 등 i18n 텍스트) vs 실제 `def.apply()` 적용값
  - 참고: 260706에서 "공격속도" 스탯이 desc는 공속인데 구현은 쿨감(`cd`)과 사실상 동일 로직이라는
    미해결 논의가 있었음 — 이것도 이번 점검에서 같이 정리 필요

### 점검 방식 (제안)
- 각 카드 타입(`upgrade`/`ascend`/`special`/`new_sub`/`stat`)별로 `desc` 텍스트 생성 코드와
  실제 효과 적용 코드(`WeaponInstance.upgrade()`, `def.apply()` 등)를 나란히 대조
- `lang.js`/`weapons.js`의 `WEAPON_I18N` 텍스트도 같이 대조 (한/영 둘 다)
- 괴리 발견 시 텍스트를 실제 효과에 맞게 수정 (효과 쪽을 텍스트에 맞추는 게 아니라 텍스트를 실제 코드 기준으로 갱신)

### To-Do (PC 세션에서 점검·구현)
- [ ] 주무기 레벨업 카드 텍스트 전수 점검 (강화/각성/초월 단계별)
- [ ] 보조무기 카드 텍스트 전수 점검
- [ ] 스탯 카드 텍스트 전수 점검 (특히 "공격속도" vs 쿨감 표기 문제 같이 정리)
- [ ] 괴리 발견 목록 정리 → 텍스트 일괄 수정
- [ ] 한/영 i18n 텍스트 동시 수정 (키 172쌍 일치 유지)

---

## 12. 버그: 로비 하단 네비 "Stage" 탭에 보라색 바가 항상 고정 표시 (설계 확정, 코드 미반영)

### 증상
로비 하단 네비게이션에서 어느 탭을 선택하든 상관없이 **Stage 탭 위에만 보라색 상단 바 + 은은한 보라색 배경**이 항상 붙어있음. 원래 의도는 "현재 선택된 탭 강조"였는데, 실제로는 Stage 탭에 고정.

### 원인 (`css/style.css`)
```css
.nav-btn-stage {
  background: linear-gradient(180deg, rgba(112,64,192,0.2) 0%, transparent 100%);
  position: relative;
}
.nav-btn-stage::after {
  content: '';
  position: absolute;
  top: 0; left: 0; right: 0;
  height: 2px;
  background: var(--spirit-lt);
}
```
`.nav-btn-stage`는 "현재 활성 탭"을 나타내는 상태 클래스가 아니라, **Stage 버튼 자체에 고정으로 붙는 식별자 클래스**로 만들어져 있음. 그래서 다른 탭(Clinic/Shrine/Totem Hall/Pet/Character)을 눌러 이동해도 Stage 버튼의 보라색 장식은 그대로 남아있고, 다른 탭에는 강조 효과가 아예 안 생김.

### 수정 방향 (확정)
`.nav-btn-stage` 같은 버튼별 고정 클래스 대신, **현재 활성 탭에 동적으로 붙는 `.active` 클래스** 기반으로 전환.
- `lobby.js`의 하단 네비 렌더링 함수에서 각 버튼 생성 시 `현재화면===탭id ? 'active' : ''` 클래스를 부여
- CSS는 `.nav-btn-stage` 대신 `.nav-btn.active`에 보라색 그라데이션+상단 바 스타일 적용
- `.nav-btn-stage`와 `::after` 규칙 제거 (또는 `.active`로 이름 변경)

### To-Do (PC 세션에서 구현)
- [ ] `style.css`에서 `.nav-btn-stage` / `.nav-btn-stage::after` → `.nav-btn.active` / `.nav-btn.active::after`로 교체
- [ ] `lobby.js`의 `bottom-nav` 렌더링 로직에서 각 탭 버튼에 현재 화면 기준 `active` 클래스 동적 부여
- [ ] 6개 탭(Stage/Clinic/Shrine/Totem Hall/Pet/Character) 전부 순회하며 탭 전환 시 보라색 바가 정확히 따라 이동하는지 실기기 검증

---

## 9. 혼돈석/순리석 던전 드랍 확률 확정 (설계 확정, 코드 미반영)

260715 #4(시즌별 특화재화 전용 던전 신설)의 미결 항목이었던 드랍량을 확정. `items.js` 기존 코드 확인 결과, 별도 설계가 필요 없는 것으로 판명.

### 발견 — 기존 4개 재화던전 전부 동일 확률 사용 중

`items.js`의 `rollDrops()`:
```js
} else if (rewardMode) {
  rates.gold = 0;       // 특화 던전: 골드 드랍 차단
  rates.bigGold = 8;    // 특화 재화 드랍률 8%
}
```

강화석/천운석/천령과/태극석 던전 전부 **재화 종류 상관없이 `rewardMode`만 세팅되면 공통 8% 확률**로 처리되는 구조. 재화별 개별 튜닝이 애초에 존재하지 않음.

### 확정 방향

혼돈석/순리석 던전도 동일 패턴 재사용 — **신규 드랍률 설계 불필요**, `rewardMode: 'hondonseok'` / `rewardMode: 'sullriseok'`만 세팅하면 기존 로직이 자동으로 8% 처리.

### To-Do (코드 구현 시)
- [ ] `DUNGEON_DEFS`에 혼돈석/순리석 던전 항목 추가 (260715 #4 구조 그대로, `rewardMode`만 신규 문자열)
- [ ] `unlock.js`에 `dungeon_hondonseok`(챕터30 클리어)/`dungeon_sullriseok`(챕터40 클리어) 해금 조건 추가
- [ ] `lang.js`의 `Lang.currency`/`Lang.dungeon`에 혼돈석/순리석 던전 이름 텍스트 추가
- [ ] `saveData`에 `hondonseok`/`sullriseok` 필드 존재 확인 (7번·8번에서 이미 추가 예정이면 중복 스킵)
- [ ] 코드 로직 수정 없음 확인 — `rollDrops()`는 `rewardMode` 값 무관하게 truthy 체크만 하므로 그대로 작동
